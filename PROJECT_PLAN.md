<div dir="rtl">

# خطة تنفيذ المشروع — Tawfeer Online

هذا المستند يقسّم بناء المنصة إلى **مراحل متسلسلة**، كل مرحلة تُبنى فوق ما قبلها وتُنتج نظامًا قابلًا للتشغيل والاختبار.

**المبدأ:** ننفّذ مرحلة واحدة في كل مرة، ونعتمدها قبل الانتقال للتالية.

---

## نظرة عامة على المراحل

| المرحلة | الاسم | الهدف الرئيسي | الحالة |
|:------:|-------|---------------|:------:|
| 0 | التخطيط والتوثيق | إعداد الوثائق والقرارات الأساسية | ✅ مكتملة |
| 1 | التأسيس (Foundation) | مشروع Laravel + قاعدة البيانات + المصادقة + الصلاحيات + الأسس المعمارية | ✅ مكتملة |
| 1.5 | تحليل الأعمال والتصميم | قواعد العمل، الرحلات، الأحداث، التقارير، عقد API، تصميم Phase 2، القرارات | ✅ مكتملة |
| 2 | الكتالوج والمخزون | المنتجات، الفئات، المخزون، المستودعات (27 كيانًا، 6 دفعات) | ⬜ جاهزة للبدء |
| 3 | المتجر والطلبات | الواجهة، السلة، الدفع، الطلبات | ⬜ |
| 4 | المشتريات والموردون | أوامر الشراء، الموردون، استلام البضاعة | ⬜ |
| 5 | المحاسبة | دليل الحسابات، القيود، الفواتير، المدفوعات | ⬜ |
| 6 | CRM | العملاء المحتملون، المتابعات، الفرص البيعية | ⬜ |
| 7 | المسوّقون والعمولات | الإحالات، احتساب العمولات، المدفوعات | ⬜ |
| 8 | الصندوق الموحّد | تكامل WhatsApp / Messenger / Instagram | ⬜ |
| 9 | العروض والولاء | الكوبونات، الخصومات، النقاط | ⬜ |
| 10 | التقارير والتحليلات | لوحات KPI، التقارير المتقدمة | ⬜ |
| 11 | التحسين والنشر | الأداء، الأمان، الإطلاق | ⬜ |

> **التسليم الفعلي (تحديث الخريطة):** نُفِّذت المنصّة بدفعات مفصّلة: الأساس (1) → الكتالوج/المخزون (2.1–2.10) → المتجر والطلبات (3.1–3.5) → العمليات (4.1–4.7: بيع مُساعد، عمولات، توصيل، مرتجعات، تسويات، تقارير) → المستودع (5) → **النمو والذكاء (6): محتوى المنتج بالذكاء الاصطناعي، محرّك التوصيات، أتمتة التسويق، لوحات KPI موسّعة** ✅. ثم جاهزية الإنتاج (11).
>
> **قيود النطاق المُعتمدة (تُستبعَد نهائيًا):** عمل واحد · مستودع رئيسي واحد · **بلا فروع** · **بلا تعدد مستودعات** · **بلا تكامل Aliphia** · **بلا نقاط ولاء** · **بلا كوبونات ذكية** · **بلا وحدة مطالبات توصيل**.

---

## المرحلة 0 — التخطيط والتوثيق 🟢

**المخرجات:**
- [x] فحص المستودع والتحقق من Git.
- [x] `README.md`
- [x] `PROJECT_PLAN.md`
- [x] `REQUIREMENTS.md`
- [x] `DATABASE_DESIGN.md`
- [x] `CLAUDE.md`
- [x] `DEPLOYMENT.md`

---

## المرحلة 1 — التأسيس (Foundation) ✅

**الهدف:** أساس تقني صلب يعمل عليه كل ما بعده.

> **مُنجَز:** Laravel 13 + مصادقة (Breeze) + Sanctum + spatie/permission + RTL/عربي +
> أسس معمارية (Branch، UUID، Audit، Settings، Statuses، Modules، طبقة تكامل).
> 33 اختبارًا ناجحًا، وتحقّق تشغيلي عبر الخادم والـ API.

**النطاق:**
1. تهيئة مشروع **Laravel (أحدث إصدار مستقر)** داخل المستودع.
2. إعداد `.env` والاتصال بـ MySQL (مع بديل محلي للتحقق داخل الحاوية).
3. إعداد Tailwind CSS مع دعم RTL والخط العربي.
4. التوطين (عربي/إنجليزي) وبنية ملفات اللغة.
5. المصادقة (تسجيل/دخول/استعادة كلمة المرور) + **Sanctum** (API-First).
6. نظام الأدوار والصلاحيات (`spatie/laravel-permission`): مدير، موظف مبيعات، محاسب، أمين مستودع، مسوّق، عميل.
7. الهيكل المعماري للوحدات (Modules) واتفاقيات الكود.
8. Layout أساسي للوحة التحكم + Layout للمتجر.
9. Seeder للمستخدم المدير الافتراضي والأدوار.

**أسس معمارية تُرسَّخ الآن (من `ARCHITECTURE.md`):**
10. جدول `branches` + فرع افتراضي (Multi-Branch Ready).
11. تريتة `HasUuid` للمعرّفات الخارجية.
12. تريتة `Auditable` + جدول `audit_logs` المركزي.
13. جدول `settings` + طبقة `Settings` مع cache.
14. جداول الحالات القابلة للإدارة (`order/payment/shipment_statuses`) + بذورها.
15. تريتات/قواعد إلزامية: `SoftDeletes`، Decimal للمبالغ، غلاف `DB::transaction()` للعمليات الحسّاسة، عقود التكامل (Contracts).
16. أساس **API** (بادئة `/api`، Sanctum، API Resources موحّدة).

**معايير القبول:**
- تشغيل المشروع محليًا بنجاح.
- تسجيل الدخول بأدوار مختلفة ورؤية لوحة مناسبة لكل دور.
- الترحيلات (migrations) الأساسية تعمل، ووجود فرع افتراضي وحالات مزروعة.
- سجلّ التدقيق يلتقط عملية تجريبية، والإعدادات تُقرأ من قاعدة البيانات.

---

## المرحلة 1.5 — تحليل الأعمال والتصميم ✅

**الهدف:** ترجمة رؤية الأعمال إلى قرارات وتصميم مُلزِم قبل كتابة كود Phase 2.

**المخرجات (13 وثيقة في `docs/`):** `DECISIONS` · `BUSINESS_RULES` · `USER_JOURNEYS` · `EVENTS` · `AUTOMATIONS` · `REPORTS` · `API_CONTRACT` · `UI_NAVIGATION` · `APPROVAL_WORKFLOWS` · `NOTIFICATION_MATRIX` · `PHASE_2_DESIGN` · `DATA_DICTIONARY` · `DESIGN_REVIEW`.

> الخلاصة الكاملة والأسئلة المفتوحة في [`docs/DESIGN_REVIEW.md`](docs/DESIGN_REVIEW.md).

---

## المرحلة 2 — البناء التدريجي (feature-by-feature)

**المنهجية:** كل ميزة **تُنفَّذ كاملة → تُختبر → تُلتزم → تُدفع → تُراجَع** قبل الانتقال للتالية. لا يُبنى النظام دفعة واحدة.

| المرحلة الفرعية | المحتوى | الحالة |
|:--------------:|---------|:------:|
| **2.1** | الفروع، المستودعات، مواقع التخزين | ✅ مكتملة |
| **2.2** | الفئات، العلامات، الوحدات، السمات، القيم، الوسوم | ✅ مكتملة |
| **2.3** | المنتجات، الوسائط، التصنيف، الحالة/الظهور، SEO/البحث | ✅ مكتملة |
| **2.4** | المخزون، الحركات، الدفتر، الحجوزات، التسويات | ✅ مكتملة |
| **2.5** | المشتريات (موردون، أوامر شراء، استلام، مرتجعات) | ✅ مكتملة |
| **2.6** | طلبات البيع (دورة حياة + حجز/خصم مخزون) | ✅ مكتملة |
| **2.7** | الشحن (جغرافيا + شحنات + طبقة تكامل التوصيل) | ✅ مكتملة |
| **2.8** | المدفوعات (طبقة تكامل مزوّدين + COD/تحويل) | ✅ مكتملة |
| **2.9** | المحاسبة (قيد مزدوج + دفتر غير قابل للتعديل) | ✅ مكتملة |
| **2.10** | CRM/العملاء (تعدد هواتف/عناوين/ملاحظات + حظر/دمج) | ✅ مكتملة |
| 2.11 | التقارير | ⬜ |
| 2.12 | لوحة المعلومات | ⬜ |

**التصميم المرجعي:** [`docs/PHASE_2_DESIGN.md`](docs/PHASE_2_DESIGN.md) و[`docs/DATA_DICTIONARY.md`](docs/DATA_DICTIONARY.md).

### ✅ المرحلة 2.1 — الفروع/المستودعات/المواقع (مكتملة)
- **الهجرات:** توسعة `branches` (email, tax_number, default_currency_id, default_warehouse_id, timezone)، `branch_settings`، `warehouses`، `warehouse_locations`.
- **النماذج:** `Warehouse`, `WarehouseLocation`, `BranchSetting` + توسعة `Branch` (علاقات، مصنع).
- **API:** CRUD كامل لـ `/api/v1/branches` و`/warehouses` و`/warehouses/{id}/locations` (ربط UUID، ربط مُنطاق، معاملات ذرّية).
- **الصلاحيات:** 12 صلاحية دقيقة `settings.{branches,warehouses,warehouse_locations}.{view,create,update,delete}` (ADR-021)، مزروعة وموزّعة على الأدوار.
- **الأسس:** UUID، Soft Delete، Auditable، منع السالب لكل مستودع، مستودع افتراضي واحد لكل فرع.
- **الاختبارات:** 25 اختبار API (المجموع 58 ✅). تحقّق حيّ عبر الخادم.

### ✅ المرحلة 2.2 — الكتالوج المرجعي (مكتملة)
- **الوحدة:** `app/Modules/Catalog` (نماذج، خدمات، سياسات، مزوّد خدمة).
- **الكيانات (6):** الفئات (شجرية، uuid، soft-delete، تدقيق)، العلامات (uuid، soft-delete، تدقيق)، الوحدات (تحويل)، السمات، قيم السمات، الوسوم.
- **API:** REST كامل تحت `/api/v1` (فلترة/بحث/ترتيب/ترقيم/تداخل)؛ الفئات/العلامات بـ uuid والمراجع بـ id (ADR-002).
- **منطق الأعمال في الخدمات:** توليد slug فريد، منع الدورات الشجرية، حذف آمن (منع حذف فئة ذات فروع/وحدة أساس مرجعية).
- **الصلاحيات:** 20 صلاحية `catalog.{resource}.{action}` (ADR-021) + **سياسات (Policies)**.
- **واجهة إدارة عربية RTL:** قوائم/نماذج متجاوبة متسقة مع اللوحة، مع توطين إنجليزي.
- **الاختبارات:** 43 اختبارًا جديدًا (API + واجهة) → **103 ناجحة**؛ تحقّق حيّ عبر الخادم.

### ✅ المرحلة 2.3 — المنتجات ووسائطها (مكتملة)
- **الجداول (4):** `products` (§16 + توسعة ADR-023)، `product_images`، `product_tag_links`، `product_attribute_links`.
- **التوسعة (ADR-023):** تسمية/وصف ثنائيا اللغة، حالة تحريرية (draft/active/archived)، ظهور، مميّز، ترتيب، SEO، بيانات بحث؛ `is_active` مُشتقّ من الحالة.
- **الوسائط:** رفع صور إلى قرص public، صورة أساسية واحدة تلقائيًا، ترقية عند الحذف.
- **العلاقات:** فئة/علامة/وحدة (belongs-to)، وسوم/سمات (M:N)، صور (has-many).
- **API:** REST كامل + وسائط متداخلة؛ فلترة (فئة/علامة/وسم/حالة/ظهور/مميّز/نوع)، بحث (اسم/SKU/باركود/كلمات بحث)، ترتيب، ترقيم. حقول التكلفة محجوبة بـ `pricing.view_cost` (ADR-013).
- **الصلاحيات:** `catalog.products.*` + `pricing.view_cost` (Policies).
- **واجهة إدارة RTL:** قائمة + نموذج شامل (ثنائي اللغة، تصنيف، وسوم/سمات، حالة، SEO) + معرض صور.
- **الاختبارات:** 33 اختبارًا جديدًا (API + وسائط + واجهة + وحدة الخدمة) → **136 ناجحة**؛ تحقّق حيّ.
- **مستثنى عمدًا (خارج 2.3):** المتغيّرات، محرّك التسعير، المخزون — تُبنى في مراحلها.

### ✅ المرحلة 2.4 — المخزون (مكتملة)
- **الأساس (ADR-024):** `product_variants` كأساس للمخزون؛ متغيّر افتراضي تلقائي لكل منتج.
- **الجداول:** `inventory_stocks` (دلاء + WAC)، `inventory_movements` (append-only، 10 أنواع)، `inventory_ledger` (append-only، رصيد جارٍ + WAC)، `stock_reservations`، `stock_adjustments` (+بنود).
- **وحدة Inventory:** نماذج، خدمات، سياسات، مزوّد خدمة.
- **المحرّك (`InventoryService`):** استلام/صرف/تحويل، **إعادة حساب WAC** (ADR-005)، **منع السالب** (ADR-007a)، كل عملية داخل `DB::transaction` مع قفل صف، تكتب حركة + قيد دفتر.
- **الحجوزات (`ReservationService`):** حجز/تحرير على دلو `reserved`، منع الحجز فوق المتاح.
- **التسويات (`StockAdjustmentService`):** draft→pending_approval→approved→posted؛ ترقيم `ADJ-{YYYY}-{seq}`؛ الترحيل يطبّق الفروقات.
- **API:** `/api/v1/inventory/*` (أرصدة/حركات/دفتر/استلام/صرف/تحويل/حجوزات/تسويات)؛ حقول التكلفة محجوبة بـ `pricing.view_cost`.
- **الصلاحيات:** 14 صلاحية `inventory.*` (ADR-021) + سياسات.
- **واجهة إدارة RTL:** الأرصدة، سجلّ الحركات، الحجوزات، العمليات (استلام/صرف/تحويل)، التسويات (إنشاء/اعتماد/ترحيل).
- **الاختبارات:** 27 اختبارًا جديدًا (عمليات + حجوزات + تسويات + وحدة الخدمة + واجهة) → **164 ناجحة**؛ تحقّق حيّ.
- **مستثنى عمدًا:** مستند التحويل (§27)، إدارة المتغيّرات الكاملة، ربط الطلبات — تُبنى لاحقًا.

### ✅ المرحلة 2.5 — المشتريات (مكتملة)
- **الوحدة:** `app/Modules/Purchasing` (8 نماذج، 4 خدمات، 4 سياسات، مزوّد خدمة يسجّل السياسات).
- **الجداول (8):** `suppliers` (uuid، soft-delete، تدقيق)، `supplier_contacts`، `purchase_orders` (+`purchase_order_items`)، `goods_receipts` (+`goods_receipt_items`)، `supplier_returns` (+`supplier_return_items`). مفاتيح أجنبية، قيود فريدة مركّبة، فهارس؛ FKs جغرافيا/عملة مؤجّلة (ADR-025).
- **أوامر الشراء (`PurchaseOrderService`):** draft→pending_approval→approved→partially_received→received→closed (+cancelled)؛ ترقيم `PO-{YYYY}-{seq}`؛ قابل للتعديل حتى الاعتماد (BR-PUR-02)؛ منع الإلغاء بعد بدء الاستلام (BR-PUR-12).
- **استلام البضاعة (`GoodsReceiptService`):** draft→posted (+cancelled)؛ ترقيم `GRN-{YYYY}-{seq}`؛ لا استلام قبل الاعتماد (BR-PUR-03)؛ **الترحيل يرفع المخزون حصريًا عبر `InventoryService::receive`** مع توزيع التكلفة المحمّلة بنسبة قيمة السطر (BR-PUR-06)، وتحديث الكميات المستلمة وحالة الأمر (BR-PUR-05).
- **مرتجعات الموردين (`SupplierReturnService`):** draft→approved→posted (+cancelled)؛ ترقيم `PRET-{YYYY}-{seq}`؛ **الترحيل يخفّض المخزون حصريًا عبر `InventoryService::purchaseReturn`** (نوع حركة `purchase_return_out` — ADR-025، BR-PUR-11).
- **التكامل مع المخزون:** لا تُحدَّث جداول المخزون مباشرةً؛ كل زيادة/خفض يمرّ عبر محرّك المخزون (WAC، منع السالب، حركة + قيد دفتر). مرتجع يتجاوز المتاح يُرفض.
- **API:** `/api/v1/purchasing/*` (موردون CRUD؛ أوامر CRUD + submit/approve/cancel/close؛ استلام index/show/store/post؛ مرتجعات index/show/store/approve/post)؛ حقول التكلفة محجوبة بـ `pricing.view_cost` (ADR-013).
- **الصلاحيات:** 18 صلاحية `purchasing.*` (ADR-021) + سياسات؛ `PurchasingPermissionSeeder` (المدير/المدير التنفيذي كامل، المستودع تشغيلي، المحاسب قراءة).
- **واجهة إدارة RTL:** الموردون (قائمة/نموذج بجهات اتصال)، أوامر الشراء (قائمة/نموذج بنود ديناميكية/عرض بدورة الحياة)، الاستلام (اختيار أمر/استلام/ترحيل)، المرتجعات (إنشاء/اعتماد/ترحيل)؛ رابط تنقّل «المشتريات».
- **الاختبارات:** 24 اختبارًا جديدًا (موردون + دورة أمر الشراء + ترحيل الاستلام يزيد المخزون + ترحيل المرتجع يخفّض المخزون + منع التحوّلات غير الصالحة + التفويض + حجب التكلفة + واجهة) → **188 ناجحة**؛ تحقّق حيّ كامل عبر الخادم.
- **مستثنى عمدًا:** فواتير الموردين والمدفوعات (تُبنى في المحاسبة/المدفوعات)، أوامر الشراء متعددة المستودعات.

### ✅ المرحلة 2.6 — طلبات البيع (مكتملة)
- **التصميم أولًا:** لعدم وجود مخطط جداول مجمّد للطلبات (كانت مؤجّلة لـ"Phase 3")، أُنتجت وثيقة `docs/PHASE_2_6_SALES_ORDERS_DESIGN.md` واعتُمدت قبل البناء، نازلةً بقواعد ADR-009/010/010a وBR-ORD-01…18 إلى مخطط تنفيذي (ADR-026).
- **الوحدة:** `app/Modules/Sales` (نماذج، خدمة، سياسة، مزوّد خدمة).
- **الجداول (3):** `orders` (uuid، soft-delete، تدقيق؛ لقطة عميل + `customer_id` مؤجّل بلا FK)، `order_items` (تجميد سعر البند — BR-ORD-18؛ `qty_reserved`/`qty_shipped`)، `order_status_history` (append-only — BR-ORD-09).
- **آلة الحالات (`OrderService`):** `draft → confirmed → stock_reserved → preparing → ready_to_ship → shipped → delivered` (+`cancelled`)؛ تقرأ المفاتيح من `order_statuses` (وُسِّعت لمفردات ADR-010 الكاملة 18 حالة)؛ منع الانتقالات غير القانونية.
- **تكامل المخزون (ADR-009، حصريًا عبر خدمات المخزون):** حجز عند `stock_reserved`؛ **استهلاك + خصم نهائي بـ WAC (COGS)** عند `shipped`؛ تحرير عند الإلغاء قبل الشحن. أُضيفت `ReservationService::consume` (تنفيذ مفردة `consumed` المجمّدة).
- **API:** `/api/v1/sales/orders` (CRUD + confirm/reserve/prepare/ready/ship/deliver/cancel).
- **الصلاحيات:** 9 صلاحيات `sales.orders.*` (ADR-021) + سياسة؛ `SalesPermissionSeeder` (المدير كامل، المبيعات حتى الحجز/الإلغاء، المستودع الشحن/التسليم، المحاسب قراءة).
- **واجهة إدارة RTL:** قائمة الطلبات، نموذج إنشاء (بنود ديناميكية)، صفحة عرض بأزرار الانتقالات وسجلّ الحالات؛ رابط تنقّل «المبيعات».
- **الاختبارات:** 17 اختبارًا جديدًا (دورة الحياة وأثرها على المخزون وفق ADR-009 + الإلغاء يحرّر الحجز + منع الانتقالات + تجميد السعر + التفويض + واجهة) → **206 ناجحة**؛ تحقّق حيّ كامل.
- **مستثنى عمدًا (بخُطّافات):** محرّك التسعير، الضريبة، الشحن (2.7)، المدفوعات (2.8)، الاعتراف المحاسبي (2.9)، العمولة، المرتجعات، CRM/العملاء (2.10)، قنوات ويب/POS.

### ✅ المرحلة 2.7 — الشحن والجغرافيا وطبقة تكامل التوصيل (مكتملة)
- **التصميم أولًا (ADR-027):** لعدم وجود مخطط جداول مجمّد للشحنات، أُنتجت وثيقة `docs/PHASE_2_7_SHIPPING_DESIGN.md` واعتُمدت (مع تعديل: جدول تعيين مزوّدين متعدد بدل حقول مضمّنة) قبل البناء.
- **الجغرافيا (§3–6 بحذافيرها):** `governorates`/`cities`/`areas` (مرجعية) + `shipping_zones` (uuid/soft-delete/auditable) + جدولا ربط كثير-لكثير؛ بذرة أساسية.
- **التكامل متعدد المزوّدين:** سجلّ `delivery_providers` + `geo_provider_mappings` (polymorphic) — تعيين السجلّ المحلي الواحد لعدّة مزوّدين؛ المحلي مرجع النظام؛ تعطيل/استبدال دون فقد بيانات.
- **الشحنات (`Shipping`):** `shipments` (+`shipment_events` append-only) بلقطة عنوان ومعرّفات مُعيَّنة ولقطة تكلفة؛ آلة حالات `not_shipped→in_transit→out_for_delivery→delivered` (+failed، وحالات تشغيلية) تُكمل جزء التوصيل وتُزامن حالة الطلب (دون تكرار خصم المخزون).
- **طبقة التكامل (المبدأ 13):** 3 عقود + Null Drivers + `config/shipping.php`؛ مُحلّل تكلفة بأولوية/احتياط (يدوي/مراجعة فعّالان، الباقي مؤجّل)؛ لقطة تكلفة على الشحنة والطلب.
- **الصلاحيات:** 9 صلاحيات `shipping.*`/`settings.geography.*` (ADR-021) + سياسة؛ `ShippingPermissionSeeder`.
- **واجهة إدارة RTL:** الشحنات (قائمة/إنشاء من طلب مشحون/عرض بأزرار انتقالات وسجلّ أحداث) + عرض الجغرافيا؛ رابط تنقّل «الشحن».
- **الاختبارات:** 19 اختبارًا جديدًا (دورة الشحنة وتزامن الطلب + منع شحنة قبل الشحن + شحنة واحدة لكل طلب + تجاوز التكلفة بصلاحية + تعيين متعدد المزوّدين + منع الانتقالات + التفويض + واجهة) → **227 ناجحة**؛ تحقّق حيّ كامل.
- **مستثنى عمدًا (بخُطّافات):** محرّك تسعير الشحن الفعلي، أي مزوّد توصيل محدّد، المزامنة الحيّة، المدفوعات/COD (2.8)، الاعتراف المحاسبي (2.9)، كيان العملاء/العناوين (2.10).

### ✅ المرحلة 2.8 — المدفوعات (مكتملة)
- **المعمارية (ADR-028):** طبقة Provider/Integration — عقد موحّد `PaymentProviderInterface` + `PaymentProviderManager` (النقطة الوحيدة لإنشاء مزوّد، تربط الطريقة بـ Driver من `config/payments.php`)؛ المزوّدون حصريًا عبر هذه الطبقة (لا استدعاء مباشر). إضافة مزوّد = صفّ طريقة + Driver، دون لمس منطق الطلب/الدفع.
- **الوحدة:** `app/Modules/Payment` (نماذج، خدمة، سياسة، مزوّد). Drivers: `OfflinePaymentProvider` (COD/تحويل — فعّال) و`NullPaymentProvider` (بوابات غير مُهيّأة). HyperPay/MyFatoorah/Stripe/PayPal/Moyasar مُسجَّلة placeholders معطّلة.
- **الجداول (3 + توسعة):** `payment_methods` (سجلّ)، `payments` (uuid/soft-delete/auditable، `PAY-{YYYY}-{seq}`)، `payment_transactions` (append-only — بنية عامة لمراجع/حالات/callbacks/بيانات)؛ وأعمدة `orders.payment_status`/`amount_paid` المشتقّة.
- **`PaymentService`:** initiate (عبر مزوّد الطريقة)، capture (تحصيل COD/بوابة)، handleCallback (webhook)، refund (كلي/جزئي)، وإعادة اشتقاق حالة دفع الطلب — يدعم الدفع الجزئي/التراكمي والاسترداد.
- **API:** `/api/v1/payments/*` (methods + CRUD + capture/refund/callback). **الصلاحيات:** 5 صلاحيات `payments.*` (ADR-021) + سياسة؛ `PaymentMethodSeeder` + `PaymentPermissionSeeder`.
- **واجهة إدارة RTL:** المدفوعات (قائمة/إنشاء من طلب/عرض بأزرار تحصيل واسترداد وسجلّ عمليات)؛ رابط تنقّل «المدفوعات».
- **الاختبارات:** 19 اختبارًا جديدًا (COD تحصيل يُحدّث الطلب، دفع جزئي/تراكمي، استرداد كلي/جزئي، منع تكرار التحصيل، منع دفع طلب ملغى، بوابة null تبقى pending، حلّ Driver عبر المدير، التفويض، واجهة) → **246 ناجحة**؛ تحقّق حيّ كامل.
- **مستثنى عمدًا (بخُطّافات):** أي تكامل بوابة محدّدة، الأثر المحاسبي/الخزائن (2.9)، Idempotency-Key، إشعارات دائنة.

### ✅ المرحلة 2.9 — المحاسبة (مكتملة)
- **المعمارية (ADR-029):** محرّك قيد مزدوج؛ قيود ودفتر غير قابلة للتعديل (عكس لا حذف)؛ الأرصدة تُشتقّ من الدفتر؛ عزل المحاسبة عبر `AccountingService` وحدث `FinancialEventOccurred` + مستمعه (المتطلّبات 1–9).
- **الوحدة:** `app/Modules/Accounting` (نماذج، خدمة، سياسة، حدث/مستمع، مزوّد).
- **الجداول (4 + إعداد):** `accounts` (دليل حسابات شجري)، `fiscal_years` + `accounting_periods` (تعدد السنوات)، `journal_entries` (`JE-{YYYY}-{seq}`، draft→posted)، `journal_lines` (append-only). دليل حسابات وسنة مالية 2026 مزروعان.
- **`AccountingService`:** createEntry (متوازن، سطر مدين/دائن، حساب قابل للترحيل)، post (draft→posted، سنة/فترة مفتوحة)، reverse (قيد عاكس، لا تعديل للأصل)، recordEvent (خريطة أحداث من `config/accounting.php`)، accountBalance + trialBalance (مشتقّان من الدفتر).
- **العزل عبر الأحداث:** الوحدات تُطلق `FinancialEventOccurred`؛ مستمع متزامن يولّد القيد — التكامل مع المبيعات/المشتريات/المخزون/المدفوعات/الاسترداد/الضرائب/الشحن مستقبلي دون تعديل الكود.
- **API:** `/api/v1/accounting/*` (accounts + balance، journal-entries CRUD + post/reverse، reports/trial-balance). **الصلاحيات:** 7 صلاحيات `accounting.*` (ADR-021) + سياسة؛ للمدير والمحاسب.
- **واجهة إدارة RTL:** قيود اليومية (قائمة/إنشاء بسطور ديناميكية وفحص توازن حيّ/عرض بترحيل وعكس)، دليل الحسابات، ميزان المراجعة.
- **الاختبارات:** 24 اختبارًا جديدًا (توازن القيد، رفض غير المتوازن، عدم قابلية التعديل للمُرحّل والسطور، العكس لا الحذف، اشتقاق الرصيد من الدفتر، الخطّاف عبر الحدث يولّد قيدًا متوازنًا، ميزان المراجعة متوازن، التفويض، واجهة) → **270 ناجحة**؛ تحقّق حيّ كامل.
- **مستثنى عمدًا (بخُطّافات):** الخزائن/البنوك التفصيلية، الضريبة الفعلية، القوائم المالية الكاملة، تعدد العملات التشغيلي، ربط أحداث الوحدات فعليًا (2.10+).

### ✅ المرحلة 2.10 — CRM/العملاء (مكتملة)
- **المعمارية (ADR-030):** وحدة `app/Modules/Crm` (نماذج، خدمة، سياسة، مزوّد)؛ كل المنطق في `CustomerService`. جاهزية تكامل عبر طبقة مراسلة provider-agnostic.
- **الجداول (5):** `customers` (uuid/soft-delete/auditable؛ حقول credit_limit/loyalty_points جاهزة للربط المستقبلي؛ حظر/عالي الخطورة/عدّادات/merged_into_id) + `customer_phones` (تعدد + أساسي) + `customer_addresses` (تعدد + افتراضي، يشير للجغرافيا) + `customer_contacts` + `customer_notes` (سجلّ زمني).
- **`CustomerService`:** إنشاء/تعديل بمزامنة الأبناء، تطبيع الهاتف وكشف التكرار (BR-CUST-03/05)، ملاحظات، حظر/رفع (BR-CUST-12)، دمج (BR-CUST-14 — نقل الأبناء والطلبات + حذف ناعم).
- **الربط بالطلبات + لقطة ثابتة:** ربط اختياري لعميل عند إنشاء الطلب (يملأ customer_id ويشتقّ اللقطة)؛ **اللقطة التاريخية على الطلب ثابتة** لا تتأثّر بتعديل العميل؛ عميل محظور لا يُنشئ طلبات.
- **طبقة المراسلة (جاهزية):** `MessagingProviderInterface` + `MessagingManager` + Null Driver + `config/messaging.php` (whatsapp/email/sms/marketing) — جاهزة دون لمس منطق CRM.
- **API:** `/api/v1/crm/customers` (CRUD + notes/block/unblock/merge/duplicates). **الصلاحيات:** 6 صلاحيات `crm.customers.*` (ADR-021) + سياسة؛ `CrmPermissionSeeder`.
- **واجهة إدارة RTL:** العملاء (قائمة/نموذج بهواتف وعناوين ديناميكية/عرض بملاحظات وحظر وسجلّ طلبات)؛ رابط تنقّل «العملاء».
- **الاختبارات:** 22 اختبارًا جديدًا (إنشاء بأبناء، أساسي/افتراضي واحد، كشف تكرار بالتطبيع، حظر/رفع، دمج ينقل الأبناء ويحذف ناعمًا، ربط الطلب + ثبات اللقطة، منع طلب المحظور، طبقة المراسلة، التفويض، واجهة) → **290 ناجحة**؛ تحقّق حيّ كامل.
- **مستثنى عمدًا (بخُطّافات):** مزوّدو المراسلة الفعليون، التسعير حسب التصنيف، احتساب "عالي الخطورة" التلقائي، حساب دخول العميل.

---

## المرحلة 3 — المتجر والطلبات 🔄

**النطاق:** واجهة المتجر، عرض المنتجات والبحث، السلة، إتمام الشراء (Checkout)، بوابات الدفع، دورة حياة الطلب، الشحن.

**معايير القبول:** عميل يكمل طلبًا من التصفح حتى الدفع، وينعكس على المخزون.

**التقسيم الفرعي:** 3.1 السلة (Cart) ✅ · 3.2 إتمام الشراء (Checkout) ✅ · 3.3 كتالوج المتجر العام (Storefront) ✅ · 3.4 تجربة حساب العميل (Customer Experience) ✅ · 3.5 المصادقة والهوية (Authentication & Identity) ✅.

### ✅ المرحلة 3.1 — سلة المتجر (مكتملة)
- **المعمارية (ADR-031):** وحدة `app/Modules/Store` (نماذج + خدمة)؛ كل المنطق في `CartService`. السلة self-scoped بالمستخدم المصادَق — لا تُدار سلات الآخرين (لا سياسة/ربط لازمان).
- **الجداول (2):** `carts` (uuid/soft-delete؛ user_id/customer_id/branch_id/session_token/status) + `cart_items` (variant_id، qty decimal(15,3)، unit_price decimal(15,2)، فريد لكل (cart, variant)).
- **`CartService`:** سلة نشطة تُنشأ عند الحاجة (`forUser`)؛ إضافة/ضبط/حذف/تفريغ بند داخل معاملات؛ السعر لقطة من الكتالوج (عرض ترويجي إن وُجد وإلا التجزئة — لا محرّك تسعير)؛ فحص إتاحة Σ(on_hand − reserved)؛ رفض غير القابل للبيع (غير نشط/مخفي)؛ **لا حجز مخزون في السلة** (الحجز عند تأكيد الطلب — ADR-009).
- **API:** `/api/v1/store/cart` (GET؛ POST items؛ PUT/PATCH+DELETE items/{variant}؛ DELETE cart) عبر Sanctum؛ Form Requests للتحقّق؛ `CartResource`.
- **الاختبارات:** 9 اختبارات جديدة (منع الضيف، إنشاء سلة فارغة، تسعير من العرض الترويجي، تراكم البند، تحديث/حذف، تفريغ، رفض تجاوز المخزون، رفض غير القابل للبيع، النطاق لكل مستخدم) → **299 ناجحة**؛ Pint نظيف؛ تحقّق حيّ.
- **مستثنى عمدًا (لمراحل لاحقة):** إتمام الشراء وتحويل السلة إلى طلب (3.2)، كتالوج المتجر العام (3.3)، سلة الضيف عبر session_token (الحقل مُهيّأ)، محرّك التسعير/الكوبونات.

### ✅ المرحلة 3.2 — إتمام الشراء متعدّد الخطوات (مكتملة — مُنقَّحة بقرار المالك)
- **المعمارية (ADR-033):** `CheckoutService` **تنسيقي بحت** يحوّل السلة النشطة إلى طلب عبر الخدمات القائمة دون منطق أعمال جديد. **تنقيح المالك المُعتمد:** دعم **المصادَق والضيف**، و**جلسة إتمام متعدّدة الخطوات** بدل النقطة الذرّية الواحدة، مع **الحفاظ على معاملة الإنشاء الذرّية النهائية وبدء الدفع كما نُفّذا**، بأدنى تغييرات إضافية ودون إعادة تصميم وحدات مكتملة.
- **الهوية المزدوجة (إضافي):** طبقة `store.identity` تقبل مستخدمًا مُصادَقًا أو ضيفًا برمز سلة `X-Cart-Token` (وترفض بـ401 عند غيابهما — يحافظ على سلوك 3.1). سلة الضيف عبر `carts.session_token` القائم (بلا تغيير مخطط). `CartService::resolveActive/forGuest` مضافان (سلوك `forUser` بلا تغيير).
- **جدول إضافي واحد `checkout_sessions`:** يتراكم عليه لقطة الشحن + طريقة الدفع عبر الخطوات؛ لا تعديل لـ`orders`/`payments`/المخزون.
- **الخطوات:** `start` (يرفض الفارغة + `CheckoutStarted`) → `update` (تحديث تدريجي) → `place` (تحقّق اكتمال + إعادة توافر ثم **المعاملة المحفوظة**: `create → confirm → reserveStock` **[ينعكس على المخزون — معيار القبول]** `→ PaymentService::initiate` [COD pending] → تعليم السلة `converted` + `CheckoutCompleted`).
- **API:** `POST /store/checkout` (بدء) · `GET/PUT/PATCH /store/checkout/{session}` · `POST /store/checkout/{session}/place`. الملكية تُفحص لكل جلسة (403 عند عدم التطابق).
- **الاختبارات:** 13 اختبار إتمام (منها إتمام ضيف كامل ومنع الوصول لجلسة الغير) + سلة 3.1 (9) دون تغيير → **312 ناجحة**؛ Pint نظيف؛ تحقّق حيّ لتدفّقي المصادَق والضيف.
- **مستثنى عمدًا (نقاط امتداد موثّقة — ADR-033):** الكوبونات/العروض/الولاء، طلبات الهدايا، Save For Later/Buy Again/Express، اختيار عنوان محفوظ عبر API، تسعير الشحن الفعلي، دمج سلة الضيف مع الحساب عند تسجيل الدخول.

### ✅ المرحلة 3.3 — كتالوج المتجر العام (مكتملة)
- **المعمارية (ADR-034):** واجهة متجر مُخدَّمة من الخادم (Blade + Tailwind + Alpine)، **عربي RTL + إنجليزي**، mobile-first. قراءة عبر `StorefrontService` تُعيد استخدام نطاقات المنتج وتسعير/توافر `CartService` (لا تكرار منطق). السلة/الإتمام عبر واجهات 3.1/3.2 من العميل (ضيف/مُصادَق).
- **النطاق:** رئيسية، فئات/علامات، قائمة منتجات مع فلترة/بحث/ترتيب/ترقيم، تفاصيل منتج مع معرض صور ومواصفات وتوفّر، إضافة للسلة، تنقّل واضح للإتمام، تبديل لغة.
- **SEO:** روابط slug، meta/OG/Twitter/canonical، بيانات مهيكلة JSON-LD (Product/ItemList).
- **جاهزية التسويق (ADR-032، بلا منطق نمو):** عقد `StorefrontRecommendationProvider` + `Null` (مميّز/وصل حديثًا من الكتالوج؛ related/cross-sell/upsell/bundles/best-sellers/personalized نقاط امتداد فارغة تُخفى تلقائيًا). شارات عرض من `promo_price`؛ عتبة شحن مجاني من الإعداد.
- **جاهزية التحليلات (بلا مزوّد):** أحداث نافذة `ProductViewed/CategoryViewed/SearchPerformed/ProductAddedToCart/ProductRemovedFromCart/CheckoutStarted`.
- **UX:** بطاقات سريعة، حالات فارغة/تحميل/خطأ، معرض صور، Tajawal RTL، نماذج وتنقّل مُوسومة (aria).
- **الاختبارات:** 14 اختبار واجهة → **326 ناجحة**؛ Pint نظيف؛ بناء Vite للإنتاج ناجح؛ تحقّق حيّ RTL/موبايل وتدفّقي السلة (ضيف/مُصادَق).
- **مستثنى عمدًا (خُطّافات موثّقة):** محرّك التوصيات/العروض الفعلي، best-sellers، صفحة دخول للمتجر ودمج سلة الضيف، مزوّد تحليلات خارجي.

### ✅ المرحلة 3.4 — تجربة حساب العميل (مكتملة)
- **المعمارية (ADR-035):** طبقة تجربة عميل كاملة بجلسة الويب فوق الوحدات القائمة دون إعادة تصميم ولا تكرار منطق. `AccountService`/`WishlistService` + دوال مُضافة لـ`CustomerService`/`CartService` (إعادة استخدام).
- **النطاق:** تسجيل/دخول/خروج، **دمج سلة الضيف عند الدخول**، ملف، عناوين محفوظة (تعدّد + افتراضي)، **تاريخ ميلاد مطلوب عند التسجيل** (جاهزية)، «طلباتي» + تتبّع (مسار زمني + استطلاع حيّ) + إعادة الطلب، المفضّلة (Wishlist)، مركز إشعارات (قناة database + جاهزية قنوات خارجية عبر MessagingManager)، تفضيلات (لغة/فرع/قنوات تواصل)، إعدادات (كلمة مرور).
- **مخطط إضافي فقط:** أعمدة تجربة/تسويق على `customers`، `wishlist_items`، جدول `notifications` القياسي. لا تعديل لمخطط مكتمل.
- **RTL/موبايل/SEO/تحليلات/نمو:** صفحات RTL + إنجليزي، mobile-first، صفحات الحساب `noindex`، أحداث تحليلات، ونقاط امتداد النمو محفوظة (الميلاد/التفضيلات/القنوات/المفضّلة جاهزة للربط لاحقًا).
- **الاختبارات:** 11 اختبار جديد → **337 ناجحة**؛ Pint نظيف؛ بناء Vite ناجح؛ تحقّق حيّ.
- **مستثنى عمدًا (بلا تنفيذ — ADR-032):** الولاء/النقاط، مكافآت الميلاد، الكوبونات/العروض، التوصيات/السلة الذكية، أتمتة التسويق، تتبّع بالبثّ الحيّ.

### ✅ المرحلة 3.5 — المصادقة والهوية (مكتملة)
- **المعمارية (ADR-036):** إتمام طبقة المصادقة/الهوية فوق حساب 3.4 دون إعادة تصميم. متحكمات رفيعة، منطق في الخدمات (`SocialAuthService`/`GuestMergeService` + إعادة استخدام `CustomerService`/`CartService`).
- **المصادقة:** تسجيل/دخول (بريد **أو هاتف**/كلمة مرور)، استرجاع كلمة مرور (وسيط قياسي، واجهة متجر)، تذكّرني، خروج آمن، قبول الشروط والخصوصية.
- **الدخول الاجتماعي (Google/Facebook):** طبقة تكامل قابلة للتبديل (Socialite خلف عقد) + DTO محايد + مزوّد وهمي للاختبار؛ أزرار بعلامات رسمية على الدخول/التسجيل. الأسرار في `.env` فقط.
- **الهوية:** جدول `social_identities`؛ ربط دون تكرار حسابات (بريد موثّق قائم → ربط)؛ إكمال الملف قبل الشراء لمستخدمي الدخول الاجتماعي؛ إدارة المزوّدين (عرض/ربط/فكّ مع حماية آخر وسيلة).
- **دمج الضيف:** سلة + ربط طلبات الضيف المؤهّلة (تطابق الهاتف) + الحفاظ على العناوين/الإشعارات.
- **مخطط إضافي فقط:** `social_identities`، `users.terms_accepted_at`، `customers.primary_phone` صار nullable.
- **الاختبارات:** 12 اختبار جديد → **349 ناجحة**؛ Pint نظيف؛ بناء Vite ناجح؛ تحقّق حيّ.
- **مستثنى عمدًا (بلا تنفيذ — ADR-032):** الولاء، مكافآت الميلاد، السلة الذكية، أتمتة التسويق، التوصيات، الكوبونات، Meta Ads، Facebook Pixel، التحليلات.

---

## المرحلة 4 — العمليات (Assisted Sales · Affiliate · Delivery · Returns · Settlements · Reports) ✅

**النطاق:** يعكس سير العمل التشغيلي الفعلي. التصميم الكامل: [`docs/PHASE_4_OPERATIONS_DESIGN.md`](docs/PHASE_4_OPERATIONS_DESIGN.md) (ADR-037). يُنفَّذ بدفعات.

**التقسيم الفرعي:** 4.1 البيع المُساعد ✅ · 4.2 دفاتر العمولة/الأرباح ✅ · 4.3 عمليات التوصيل والاستثناءات ✅ · 4.4 المرتجعات/الاستبدال (RMA) ✅ · 4.5 مطالبات التوصيل ⏭️ **مُستبعَدة (تقليص نطاق)** · 4.6 التسويات المالية والمطابقة ✅ · 4.7 التقارير التشغيلية ✅. **المرحلة 4 مكتملة.**

> ملاحظة: المشتريات/الموردون أُنجزت في المرحلة 2.5؛ رُقّمت المرحلة 4 هنا لتطابق نطاق العمليات المُعتمد.

> **تقليص النطاق (بطلب المالك):** أُلغيت **المرحلة 4.5 (مطالبات التوصيل)** ولا تُبنى وحدة مطالبات مستقلّة. الأولويّات المتبقّية: **التسوية المالية (4.6) → التقارير التشغيلية (4.7) → لوحات المعلومات → جاهزية الإنتاج/الأداء/الأمان/النشر (المرحلة 11)**. تُحفَظ كل القرارات المعمارية السابقة كما هي.

### ✅ المرحلة 4.1 — البيع المُساعد وإدخال الطلبات اليدوي (مكتملة)
- **المعمارية (ADR-037):** `AssistedOrderService` يُنشئ طلبًا من قناة عبر **إعادة استخدام `OrderService::create`** (الحجز/الحالات/اللقطة) و`CustomerService` (البحث/الإنشاء بالهاتف المطبّع). لا تكرار منطق؛ متحكّم رفيع.
- **القناة والإسناد:** توسعة مفردات `orders.channel` (`whatsapp/instagram/messenger/phone/other`)؛ `orders.affiliate_id` (إسناد المسوّق؛ الكيان/المحفظة في 4.2).
- **تعديل السعر واللقطات (المتطلّب 2):** لقطات على `order_items` (retail/wholesale-cost/reason/approved-by)؛ **دفتر `order_price_changes` غير قابل للتعديل**؛ الموافقة مطلوبة تحت `min_price`/التكلفة ما لم يملك المنفّذ `sales.orders.override_price` (BR-EMP-05/BR-PRICE-06).
- **الأدوار/الصلاحيات:** دور `sales_supervisor`؛ صلاحيتا `sales.orders.{assist,override_price}` (ADR-021)؛ اعتماد/رفض تعديلات السعر للمشرف.
- **API:** `POST /api/v1/sales/assisted-orders`؛ `GET/POST .../price-changes[/{id}/approve|reject]`.
- **الأحداث:** `AssistedOrderCreated`، `OrderPriceChange{Requested,Approved,Rejected}` (نقاط امتداد).
- **الاختبارات:** 8 اختبارات → **357 ناجحة**؛ Pint نظيف.
- **مؤجّل للدفعات التالية:** دفاتر العمولة/الأرباح (4.2)، عمليات التوصيل (4.3)، RMA (4.4)، المطالبات (4.5)، التسويات (4.6)، التقارير (4.7).

### ✅ المرحلة 4.2 — دفاتر عمولة المبيعات وأرباح المسوّقين (مكتملة)
- **المحرّك (CommissionService):** استحقاق تلقائي عند `OrderDelivered` عبر مستمع؛ العمولة الافتراضية **1%** قابلة للضبط بقاعدة حسب الموظف/الفترة/الحملة/المنتج/الفئة/الفرع/الدور. أرباح المسوّق = هامش `(سعر البيع − لقطة تكلفة الجملة) × الكمية`.
- **دفاتر غير قابلة للتعديل (append-only):** `commission_entries` (حركات موقّعة: accrual/adjustment/reversal) + `commission_transitions` (سجل انتقالات الحالة) + `commission_payouts`/`commission_payout_entries`. **الرصيد مُشتقّ من الدفتر لا مخزّن**؛ حارس نموذجي يمنع تعديل الحقول المالية (`RuntimeException`).
- **دورة الحياة:** `pending → eligible → approved → paid` (+ `adjusted/reversed/cancelled`) بانتقالات مُتحقَّق منها؛ الأهلية **لا تُمنح إلا بالتسوية في 4.6** (`markEligibleForOrder`) — ليست بمجرّد الإنشاء/التسليم.
- **أسبقية القواعد (حتمية):** موظف > حملة > منتج/فئة > فرع > دور > عام (`ruleScorePriority`)؛ اختيار أعلى أولوية ضمن الفترة الفعّالة.
- **الاعتماد/الصرف:** اعتماد فردي/دفعي (finance/supervisor)؛ صرف دفعي مع مرجع؛ منع الاعتماد/الدفع المزدوج (قيد فريد `uniq_entry_paid_once` + أقفال `lockForUpdate`)؛ صرف جزئي لكل مستفيد.
- **جاهزية المرتجعات:** `adjustForReturn` (تسوية نسبية سالبة) و`reverseForOrder` (عكس كامل) — **بإضافة حركات جديدة دون مسّ التاريخ**؛ خدمات/أحداث جاهزة لـ RMA (4.4) دون تنفيذ كامل.
- **جاهزية المحاسبة:** إعادة استخدام `AccountingService`؛ **لا قيود نهائية قبل أهلية التسوية**؛ ترحيل مستقبلي idempotent.
- **الصلاحيات (ADR-021):** `commissions.{view_own,view_team,rules.manage,approve,payout,audit.view}`؛ أدوار `finance`/`sales_supervisor`.
- **الواجهات:** لوحة إدارة RTL (دفتر بتبويب الحالات، إدارة القواعد، كشف حساب المستفيد، اعتماد/صرف دفعي) + عربي/إنجليزي كامل.
- **API:** `GET /api/v1/commissions[/statement]`؛ `POST .../approve|payout`؛ `apiResource commissions/rules`.
- **الأحداث:** `OrderDelivered` (استحقاق)؛ نقاط امتداد للتسوية والمرتجعات.
- **الاختبارات:** 15 اختبارًا → **372 ناجحة**؛ Pint نظيف؛ بناء الواجهة ناجح.
- **مؤجّل للدفعات التالية:** تفعيل الأهلية بالتسوية والترحيل المحاسبي النهائي (4.6)، عمليات التوصيل (4.3)، RMA (4.4)، المطالبات (4.5)، التقارير (4.7).

### ✅ المرحلة 4.3 — عمليات التوصيل وتكامل المزوّد (مكتملة)
- **المحرّك (ADR-038):** سير عمل Opost الرسمي أصبح **دورة الحياة القانونية** للتوصيل، مع فصل تامّ: `delivery_status` (قانوني تقرأه وحدات الأعمال) عن `provider_status` (خام). التعيين مزوّد ← قانوني في `OpostDeliveryProvider` (كل منطق Opost محصور بالـDriver؛ قابل للتبديل عبر الإعداد — دعم مزوّدين متعددين دون لمس منطق الأعمال).
- **المفردات:** `draft → ready_for_pickup → picked_up → {on_hold⇄} → delivered_cod_held → funds_at_courier_accounting → closed` ومسار الإرجاع `returning_to_courier → return_in_transit → closed` + `cancelled` (قبل الاستلام). أسماء قانونية واضحة (لا لبس أسماء Opost).
- **السجلّات (append-only، منفصلة):** `delivery_status_transitions` (قانوني: سابقة/جديدة/وقت/فاعل [user/system/provider]/سبب مصنّف/ملاحظة — المتطلّب 1) و`delivery_provider_transitions` (مزوّد خام + payload + idempotency فريد للـwebhook).
- **أسباب التعليق:** مُصنّفة معتمدة (9 أسباب) تُلزَم عند `on_hold` اليدوي + **قابلة للتقرير** (تجميع).
- **CLOSE = الاكتمال المالي الوحيد:** تسوية الطلب (`settled_at`) + استحقاق العمولات (مبيعات + مسوّق) **eligible فقط** + حدث `ShipmentClosed`. **لا دفع تلقائي**؛ الاعتماد/الصرف مسؤولية المالية منفصلان (4.2). `delivered_cod_held` لا يجعلها مستحقّة.
- **الصلاحيات/الأدوار:** `shipping.delivery.{view,manage,sync,close}`؛ دور `delivery_ops`؛ `close` مقصور على المالية/الإدارة.
- **الواجهات:** API (`shipments/{s}/delivery[/transition|close|provider-status]` + `delivery/hold-reasons`) + لوحة إدارة RTL (قائمة/تصفية/سجلّان/تقرير أسباب) + عربي/إنجليزي.
- **الأحداث:** `DeliveryStatusChanged`، `ShipmentClosed` (نقطة امتداد للترحيل المحاسبي 4.6).
- **البنية التشغيلية (ADR-039):** كلّها **عبر تجريد المزوّد** (`DeliveryProviderManager` — دعم عدّة مزوّدين دون تغيير المنطق):
  - **محرّك الاستثناءات:** فئات قابلة للضبط (SLA/تصعيد/دور) + استثناءات (مسؤول/مهلة/إعادة فتح) + ملاحظات append-only + **تصعيد مجدول** (`delivery:escalate-exceptions`).
  - **بنية Webhook:** نقطة عامّة `POST /webhooks/delivery/{provider}` — تحقّق توقيع (HMAC، فشل ⇒ 401)، idempotency + منع تكرار، تسجيل كامل.
  - **مزامنة مجدولة:** `delivery:sync` (بالإعداد) — الشحنات النشطة فقط، كشف تعارضات المزوّد، إعادة محاولة الفاشلة، تدقيق.
  - **محرّك الرسوم (مستقلّ عن المزوّد):** مكوّنات (توصيل/COD/حجم/وزن/منطقة نائية/إرجاع/استبدال/إعادة/يدوي/خصم) قابلة للتوسّع + إجمالي/تفصيل حسب المالك.
  - **الخطّ الزمني الموحّد:** دمج (للقراءة) الحالة الداخلية + المزوّد + التعليق + الإجراءات + webhook + المزامنة.
- **الاختبارات:** 30 اختبارًا (15 محرّك + 15 تشغيلي) → **403 ناجحة**؛ Pint نظيف؛ بناء الواجهة ناجح.

### ✅ المرحلة 4.4 — المرتجعات والاستبدال RMA (مكتملة)
- **الوحدة (ADR-040):** `app/Modules/Returns` — إضافية بالكامل، **تُعيد استخدام** كل الخدمات دون تكرار منطق: المخزون (حركتان مُضافتان `returnToStock`/`returnToDamaged` فوق محرّك الحركات)، الاسترداد (`PaymentService::refund`)، عكس العمولة (`CommissionService::reverse/adjust`)، حالة الطلب (`OrderService::markReturned/PartiallyReturned/Exchanged`)، والشحنة المرتبطة (`ShipmentService::createLinkedShipment`).
- **الأنواع/الأسباب:** إرجاع/استبدال/إبدال؛ أسباب مُصنّفة (صنف خاطئ، تالف، ناقص، رفض العميل، مشكلة شركة التوصيل، خطأ مستودع داخلي، تغيّر الرأي، أخرى). مدعوم: إرجاع كامل/جزئي، استرداد/بلا استرداد، استبدال، إبدال فقط.
- **سير الموافقات (المتطلّب):** مبيعات → مشرف مبيعات → مستودع → قرار نهائي. حالات `return_request → approved → received → inspected → completed` (+ rejected/cancelled) محروسة.
- **الفحص والتوجيه:** لكل بند نتيجة فحص + توجيه مخزون (restock→on_hand، damaged→دلو التالف، none) — **كل حركة مسجَّلة**؛ تتبّع `returned_qty` يمنع تجاوز الإرجاع.
- **شحنة مرتبطة لا تعديل للأصل:** `kind`=return_pickup/exchange_delivery + `parent_shipment_id`.
- **التوثيق:** صور اختيارية + خطّ زمني/سجلّ حالة append-only + تدقيق كامل. صلاحيات `returns.{view,create,approve,receive,inspect,finalize,refund}`؛ API + لوحة RTL موبايل-أوّلًا.
- **الاختبارات:** 13 اختبارًا → **416 ناجحة**؛ Pint نظيف؛ بناء الواجهة ناجح.
- **مؤجّل (موثّق):** دفتر الرصيد الدائن الفعلي، وعمولة بيع البديل الجديد (عند إغلاق شحنة الاستبدال).

### ✅ المرحلة 4.6 — التسوية المالية ومطابقة المزوّد (مكتملة)
- **الوحدة (ADR-041):** `app/Modules/Settlements` — إضافية، **تُعيد استخدام** المحاسبة (`AccountingService::postEntry`) والعمولات (`markEligibleForOrder`) دون تكرار.
- **المطابقة:** بيان المزوّد (COD/رسوم/خصومات/صافٍ **مُبلَّغ**) مقابل **المحسوب** من سجلّاتنا (COD=`orders.total` لشحنة مُغلقة، الرسوم من `shipment_fee_components`)؛ كشف تباين لكل سطر وإجماليًا. حالات `draft → reconciled → posted → closed`.
- **الترحيل المحاسبي:** قيد مزدوج متوازن Dr نقد+Dr مصروف شحن+Dr خصومات = Cr ذمم شركات التوصيل (حساب `1050` مُضاف للدليل)، مرتبط بالتسوية.
- **تأكيد الاستحقاق:** عند الترحيل تُعلَّم الطلبات `settled_at` ويُؤكَّد استحقاق العمولات (idempotent — مؤكِّد لإغلاق التوصيل في 4.3).
- **الصلاحيات:** `settlements.{view,manage,reconcile,post}` (finance/manager)؛ API + لوحة RTL.
- **الاختبارات:** 7 اختبارات → **423 ناجحة**؛ Pint نظيف؛ بناء الواجهة ناجح.
- **ملاحظة نطاق:** مطالبات التوصيل (4.5) مُستبعَدة؛ الخصومات المطالبية تُعالَج كبنود سطور عامّة.

### ✅ المرحلة 4.7 — التقارير والتحليلات التشغيلية (مكتملة)
- **الوحدة (ADR-042):** `app/Modules/Reporting` — **طبقة قراءة فقط** (`ReportingService`)، تجميعات SQL فوق نفس جداول الأعمال القائمة. **لا تكرار منطق ولا كتابة** — تقرأ الدفاتر التي تكتبها خدمات 4.1–4.6.
- **اللوحات (5):** تنفيذية، مبيعات، طلبات، توصيل، مالية. **التقارير (6):** أداء موظفي المبيعات، أداء المسوّقين، أداء المنتجات، إحصاءات العملاء، إحصاءات شركات التوصيل، إحصاءات المرتجعات.
- **النطاقات:** يومي/أسبوعي/شهري/مخصّص (`DateRange`)؛ بحث/فلترة سريعة؛ رسوم أشرطة من الخادم (بلا JS)؛ متجاوبة للجوّال؛ RTL عربي/إنجليزي.
- **التصدير:** Excel = CSV (UTF-8/BOM)؛ PDF = طباعة المتصفّح (عربية RTL صحيحة، بلا اعتماديات).
- **الصلاحيات:** `can:reports.view`. **الاختبارات:** 10 → **433 ناجحة**؛ Pint نظيف؛ بناء الواجهة ناجح.

### ✅ المرحلة 4.8 — الاستيراد: العملات والتكلفة الشاملة (المرحلة ١ من ٦ — مكتملة)
- **المشكلة:** الشراء من الصين يتمّ بالرمبي/الدولار بينما النظام يحاسب بالشيكل، وتُحمَّل على البضاعة مصاريفُ (عمولة مشتريات + شحن بحري) لا يعرفها النظام — فتدخل البضاعة المخزونَ بسعر المورد المجرَّد وتظهر الأرباح أعلى من الحقيقة.
- **القرار (اقتراح المالك):** **سعران لكل بند** — السعر الحقيقي (ذمّة المورد) والتكلفة الشاملة (قيمة المخزون). فلا تُنتظر فاتورة الشحن ولا يُعاد تسعير ما بِيع.
- **الحاسبة (`ImportCostCalculator`):** المصدر الوحيد للمعادلة — سعر الوحدة ÷ سعر الصرف للدولار، + نصيبه من العمولة، + حجمه بالمتر المكعّب × تكلفة المتر المكعّب، ثم × سعر الدولار. الواجهة (Alpine) مرآةٌ للعرض الفوري، والخلفية تُعيد الحساب عند كل حفظ فلا تُصدَّق قيمةٌ قادمة من المتصفّح.
- **الحقول:** `products/product_variants.cbm`؛ ورأس الفاتورة (`currency`, `fx_rate_to_usd`, `usd_rate`, `commission_rate`, `cbm_rate_usd`, إجماليات مشتقّة)؛ وبنودها (`unit_price_foreign`, `cbm_per_unit`, `landed_unit_cost`, `landed_line_total`, `landed_is_manual`).
- **التحكّم اليدوي:** تعديل عمود التكلفة بيد المستخدم يُثبّته (`landed_is_manual`) فلا تدهسه الحاسبة عند تغيير أي سعر صرف.
- **التوافق الرجعي:** الفاتورة المحلية تُترك بلا أسعار صرف فتبقى الحسابات كما كانت حرفيًا (التكلفة كما تُكتب، والتكلفة الشاملة تساويها). سعر صرف واحد بلا الآخر مرفوض، وأسعار الصرف على فاتورة بالعملة الأساسية مرفوضة (تحويلٌ خاطئ بصمت).
- **حدّ المرحلة عمدًا:** إدخال وحساب وعرض فقط — الترحيل المحاسبي لم يتغيّر فيها. الفصل مقصود ليُتحقَّق من الأرقام قبل مسّ الدفاتر.
- **الاختبارات:** 9 وحدة (المعادلة) + 17 وظيفية (الاشتقاق، الاحتياط لحجم كرت الصنف، التعديل اليدوي، الإجماليات بالعملات الثلاث، رفض المدخلات المتناقضة).

### ✅ المرحلة 4.9 — الاستيراد: الترحيل المزدوج والحساب الوسيط (المرحلة ٢ من ٦ — مكتملة)
- **الحساب الجديد:** `2110` «مصاريف استيراد مستحقة» (خصم) — مُضاف إلى `ChartOfAccountsSeeder` **وإلى هجرة بيانات** فتضمن قواعدُ البيانات القائمة وجودَه بالترقية بلا إعادة زرع يدوية.
- **القيد:** مدين المخزون بالتكلفة **الشاملة** [+ ضريبة] / دائن ذمم المورد بالسعر **الحقيقي** / دائن `2110` بالفرق. متوازن في الحالتين: بضريبة وبلا ضريبة.
- **المخزون:** البضاعة تدخل بالتكلفة الشاملة لا بسعر المورد — فيحمل متوسطُ التكلفة (WAC) مصاريفَه ويصحّ ربحُ ما يُباع لاحقًا. والصنف الجديد المُعرَّف من الفاتورة يأخذ التكلفة الشاملة في بطاقته وحجمَه من البند.
- **الاتجاه من الإشارة:** تكلفة يدوية أقلّ من سعر المورد تقلب الفرق مدينًا. ولا يُضاف سطر الاستحقاق بفرق أصغر من قرش — محرّك القيد يرفض سطرًا صفريًا أصلًا.
- **دورة الحياة:** العكس والحذف والتعديل تمرّ كلها على `postingLines` نفسها، فيعود الحساب الوسيط إلى الصفر تلقائيًا بلا مسار خاص.
- **التوافق الرجعي:** الفاتورة المحلية تُرحَّل بسطرين كما كانت — لا حساب وسيط ولا فرق.
- **الاختبارات:** 14 وظيفية (توازن القيد، وجهة كل طرف، دخول المخزون بالتكلفة الشاملة، انقلاب الإشارة، غياب السطر بلا فرق، العكس والحذف والتعديل، الصنف الجديد).

### ✅ المرحلة 4.10 — الاستيراد: الشحنة وفاتورة المصاريف وإغلاق الفرق (المرحلة ٣ من ٦ — مكتملة)
- **المشكلة التي تحلّها:** الحساب الوسيط كان رقمًا مجمَّعًا — يُرى إجماليه ولا يُعرف أيّ شحنة تخصّه ولا متى يُغلق.
- **`import_shipments`:** كيانٌ لكل كونتينر (`CNTR-{YYYY}-{seq}`) تُربط به فاتورةُ البضاعة وفواتيرُ المصاريف التي تصل بعدها بأشهر. الاسم `import_shipments` لا `shipments` — الأخير لشحنات التوصيل للعملاء.
- **نوع الفاتورة (`kind`):** `expenses` تُدين الحساب الوسيط لا المخزون، ولا تُدخل بضاعة ولا تُنشئ أصنافًا، ولا تُحمَّل عليها عمولةٌ ولا شحن (وإلا حُمّلا مرّتين). وتُرفض بلا شحنة — بغيرها لا يُعرف أيّ تقديرٍ تُطفئ. تُعيد استخدام محرّك العملات نفسه، ففاتورة الشحن بالدولار تُدخَل كما هي.
- **شاشة الإغلاق:** التقدير مقابل الفعلي، الفرق ونسبته، حدّ تسامح 3٪ يُنبّه فوقه، ونسبة المُباع — **مُعلَّمة صراحةً بأنها تقديرية** لأن المخزون لا يُتتبَّع بالدفعات.
- **الإغلاق:** يُقفل الباقي في `5050` «فروق تقدير تكاليف الاستيراد» (مُضاف للزارع ولهجرة بيانات). الاتجاه من الإشارة، وبفرقٍ دون القرش تُغلق بلا قيد.
- **إعادة الفتح:** تعكس قيد الفرق ويعود الحساب الوسيط لحاله — الإغلاق قرارُ بشرٍ يُخطئ. ولا مفتاح idempotency على قيد الفرق: الحارس هو الحالة، ومفتاحٌ ثابت كان سيمنع قيدًا جديدًا بعد إعادة فتحٍ وإغلاق.
- **الصلاحيات:** `purchasing.shipments.{view,manage,close}` — الإغلاق منفصل لأنه قيدُ نتيجة (المحاسب يملكه، المستودع يرى فقط).
- **الاختبارات:** 19 وظيفية (الترقيم، إطفاء الحساب الوسيط، عدم مسّ المخزون، الملخّص، نسبة المُباع، الإغلاق بالاتجاهين، الإغلاق بلا فرق، رفض الإغلاق المكرّر وبلا فواتير، إعادة الفتح وإعادة الإغلاق، منع الحذف، الصلاحيات، والواجهات).

### ✅ المرحلة 4.11 — الاستيراد: السداد بالعملة الأجنبية وفروق الصرف (المرحلة ٤ من ٦ — مكتملة)
- **المشكلة:** الدَّين قُيّد على المورد بسعر يوم الفاتورة ويُسدَّد بسعر يوم آخر. فلو خُصم من الذمّة ما خرج من الخزينة لبقيت قروشٌ لا تُسدَّد أبدًا؛ ولو خرج من الخزينة ما قُيّد على الذمّة لانحرف رصيد الخزينة عن الواقع.
- **الحلّ:** يُدخَل المبلغ **بالدولار** وسعرُ صرفِ **يوم الدفع**. يخرج من الخزينة `usd × سعر اليوم` (فرصيدها صحيح)، ويُطفأ من ذمّة المورد `usd × سعر الفاتورة` (فتُصفَّر بالضبط)، والفارق يُقيَّد في `5060` «فروق أسعار الصرف».
- **البنية:** سند الصرف يبقى كما هو بالمبلغ النقدي (لا مساس بـ`VoucherService`)، والفرقُ قيدٌ مستقلّ من سطرين. و`amount_paid` يُقاس بما أُطفئ من الذمّة لا بما خرج من الخزينة.
- **الحذف:** قيود فروق الصرف ليست سندات فلا تلتقطها حلقةُ عكس الدفعات — تُعكس صراحةً عند حذف الفاتورة، وإلا بقيت على المورد ذمّةٌ وهمية بمقدارها.
- **الواجهة:** مربّع الدفع يعرض فورًا: ما يخرج من الخزينة · ما يُطفأ من الذمّة · خسارة/ربح الصرف. والفاتورة تعرض المتبقّي بالدولار أيضًا.
- **التوافق الرجعي:** الدفع المحلي بلا سعر صرف يسلك المسار القديم حرفيًا، والسداد بالعملة الأجنبية مرفوض على فاتورة غير مستوردة.
- **الاختبارات:** 14 وظيفية (خسارة وربح الصرف، صحّة رصيد الخزينة، السعر المطابق بلا قيد، الدفعات الجزئية بسعرين، رفض التجاوز والسعر الصفري، الدفع المحلي دون تغيير، عكس قيد الفرق عند الحذف، والمسار والواجهة).
- **المتبقّي من الخطة (اختياري):** ٥ إعادة تقييم التكلفة من كرت المخزن · ٦ مرفقات الشحنة.

> **الخطوات التالية (المتبقّي بعد المرحلة 4):** لوحات معلومات KPI موسّعة، وجاهزية الإنتاج/الأداء/الأمان/النشر (المرحلة 11).

---

## المرحلة 5 — إدارة المستودع والمخزون (Warehouse & Inventory) ✅

> **تعزيزات إضافية فوق محرّك المخزون (2.4) دون إعادة تصميم** — ADR-043. الأرصدة/الحركات/الدفتر/الحجز/الخصم عند الشحن/الإرجاع التلقائي قائمة وتُعاد استخدامها.
- **قائم ومُعاد استخدامه (تحقّق فقط):** مستودع رئيسي واحد (WH-MAIN)، إدارة الأرصدة، الحركات، استلام المشتريات (2.5)، التسويات اليدوية (StockAdjustment)، الحجز عند إنشاء الطلب، الخصم التلقائي بعد الشحن، الإرجاع التلقائي للمخزون عند اعتماد المرتجعات (4.4)، سجلّ المخزون الكامل، التدقيق، ومزامنة التوافر (`available = on_hand − reserved`).
- **الجرد الفعلي (جديد):** `inventory_counts`/`inventory_count_items` + `InventoryCountService` (فتح بلقطة أرصدة → عدّ بالصنف/الباركود/دفعة → مراجعة → إكمال). **الإكمال يطبّق الفروق حصريًا عبر `InventoryService::adjustIn/adjustOut`** فتُسجَّل الحركة/الدفتر/التدقيق تلقائيًا.
- **تنبيهات النقص (تعزيز):** إعادة استخدام `reorder_level` القائم + تقرير/لوحة (`inventory.alerts.view`).
- **الباركود:** بحث دقيق للمتغيّر/المنتج (`WarehouseService::findByBarcode`) للمسح في الجرد.
- **العمليات الدفعية:** `InventoryBatchService::adjust` (فروق متعدّدة ذرّيًا عبر المحرّك).
- **لوحة المستودع:** مؤشّرات (أصناف/متوفّر/محجوز/متاح/قيمة/نقص/جرد مفتوح) + روابط. عربي/إنجليزي RTL متجاوب.
- **الصلاحيات:** `inventory.counts.{view,manage}`, `inventory.alerts.view`, `inventory.batch`. API + لوحة.
- **الاختبارات:** 12 اختبارًا → **445 ناجحة**؛ Pint نظيف؛ بناء الواجهة ناجح.

---

## المرحلة 6 — النمو والذكاء (AI Content · Recommendations · Marketing · KPIs) ✅

> **إضافية بالكامل فوق الوحدات القائمة دون إعادة تصميم.** وحدات جديدة `Ai`/`Recommendations`/`Marketing` + توسعة `ReportingService`. الأسرار في `.env` فقط. **خارج النطاق (بلا تنفيذ):** الولاء/النقاط، الكوبونات الذكية، Aliphia، الفروع/تعدّد المستودعات، وحدة مطالبات التوصيل.

- **محتوى المنتج بالذكاء الاصطناعي (ADR-044):** عقد `AiContentProviderInterface` + مزوّدون (Null افتراضي آمن، OpenAI معزول)؛ `AiContentService` (لا استدعاء من متحكّم/نموذج). **اقتراح دائمًا — لا كتابة/نشر تلقائي**؛ لوحة `x-admin.ai-panel` (AJAX) + زرّ «تطبيق» يدوي. سجلّ `ai_generation_logs` append-only بلا أسرار؛ حدّ معدّل + مهلة/إعادة/تراجع رشيق؛ لا اختراع مواصفات. صلاحيات `ai.content.use`/`ai.config.manage`/`ai.logs.view`.
- **محرّك التوصيات (ADR-045):** `RuleBasedRecommendationProvider` ينفّذ عقد المتجر (ADR-034) ويُبدَّل عبر الإعداد بلا تعديل المتجر؛ منطق من الكتالوج/تاريخ الطلبات فقط. **لا توصية بنافد/غير مفعّل/محذوف**؛ تثبيت/حجب يدوي (`product_recommendations`) + مصدر/سبب مسجَّل؛ تتبّع ظهور/نقر/تحويل (`recommendation_events`). أقسام على المنتج/السلة/الإتمام. صلاحيات `recommendations.{view,manage}`.
- **أتمتة التسويق (ADR-046):** الإرسال حصريًا عبر `MessagingManager`؛ `MessageDispatcher` يطبّق موافقة/حجب/ساعات هدوء/حدّ تكرار/idempotency. `Campaign` بحالات محروسة؛ تحفيز بأحداث الدومين القائمة (`MarketingEventSubscriber`) + أوامر مجدولة (عيد ميلاد/سلال مهجورة). قوالب عربي/إنجليزي، إرسال تجريبي، سجلّ تنفيذ. **الاختبارات لا ترسل حقيقةً** (`FakeMessagingProvider`). صلاحيات `marketing.campaigns.{view,manage,approve}`/`marketing.templates.manage`/`marketing.reports.view`.
- **لوحات KPI موسّعة (ADR-047):** `ReportingService::kpis(DateRange)` للقراءة فقط (مبيعات/تحصيل/ربح/قناة/موظّف/توصيل/مرتجعات/AOV/سلال مهجورة/أعلى مبيعًا/نقص مخزون/حملات/توصيات/ذكاء اصطناعي). صلاحية `kpis.view`.
- **الاختبارات:** 22 اختبارًا مركّزًا (ذكاء 6، توصيات 4، تسويق 9، KPIs 3). عربي RTL؛ كل النصوص عبر ملفات اللغة؛ Pint نظيف؛ بناء الواجهة ناجح.

---

## المرحلة 7.1 — المحاسبة التشغيلية (Cashboxes · Banks · Vouchers · Expenses · Other Income · Transfers) ✅

> **فوق محرّك القيد المزدوج القائم (ADR-029) دون إعادة بناء — ADR-050.** كل ترحيل عبر `AccountingService` (قيد متوازن، فترة مقفلة، تدقيق). الأرصدة مشتقّة من القيود دائمًا.

- **الخزائن/البنوك:** جدول `treasuries` موحّد (cash|bank)، حساب GL مخصّص فريد لكل خزينة، رصيد افتتاحي مُرحَّل كقيد، حركة، حذف آمن. حقول بنكية (IBAN/SWIFT).
- **السندات الموحّدة:** `financial_vouchers` (قبض/صرف/مصروف/إيراد/تحويل)، دورة `draft→approved→posted` + rejected/cancelled/reversed. ترحيل idempotent، **عكس لا حذف**، مرفقات، طباعة.
- **التحويلات:** صندوق/بنك بكل الاتجاهات — قيد واحد متوازن، قابل للعكس.
- **التقارير + اللوحة:** كشوف خزائن/بنوك برصيد جارٍ، تقرير سندات، حركة نقد يومية، ملخّص مصروفات/إيرادات شهري (فلاتر/CSV/طباعة) + قسم مالي في لوحة التحكّم — كلّها من القيود المُرحّلة.
- **الصلاحيات:** `accounting.{cashboxes,banks}.{view,manage}` + `accounting.{receipts,payments,expenses,income,transfers}.{view,create,approve,post}`.
- **الاختبارات:** 20 اختبارًا (ترحيل/عكس/idempotency/فترة مقفلة/صلاحيات/حذف آمن + HTTP). عربي RTL + متجاوب + سندات قابلة للطباعة.
- **المراحل المحاسبية التالية (بلا تنفيذ):** ذمم العملاء/الموردين، كشوف العملاء/الموردين، الأعمار، قائمة الدخل، الميزانية العمومية، التدفّقات النقدية، التسوية البنكية، الإقفال السنوي.

---

## المرحلة 7.2 — الميزانية اليومية (الصرف الإعلاني اليدوي) ✅

> **الخطوة الأولى قبل نظام إدارة الحملات بالذكاء الاصطناعي، لا بديلًا عنه.** غايتها بناء التاريخ الذي تُدرَّب عليه الأتمتة لاحقًا: تُدخَل أرقام Meta يدويًّا اليوم، ويصير عندك بعد أشهر سجلٌّ فعليّ لما ربح وما خسر. **خارج النطاق (بلا تنفيذ):** سحب الصرف من Meta API، Pixel وCAPI، الإيقاف/الزيادة التلقائيان، توليد المحتوى الإعلاني.

- **وحدة القرار (صنف × قناة)** لا الصنف وحده: الصنف قد يربح على صفحة ويخسر على أخرى، وجمعُهما يُخفي الخسارة داخل متوسّط. وهي بنية مدير إعلانات Meta نفسها — الحملة صفحة، والمجموعة الإعلانية صنفٌ بميزانيته.
- **إسناد آلي بلا إدخال:** الطلب ← منشئُه ← حساب البزنس لدى شركة التوصيل ← الصفحة (`ad_channels.delivery_business_id` فريد). و`orders.ad_channel_id` **لقطة** تُثبَّت لحظة الإنشاء في `Order::booted()`، فنقلُ موظفةٍ لاحقًا لا يُعيد كتابة إسناد طلباتها السابقة. أمر `ads:backfill-order-channels` لمرّة واحدة.
- **الإدخال اليومي (`ad_daily_spends`):** الصرف بالدولار وعدد المحادثات لكل (يوم، قناة، صنف) — فريد فيُحدِّث ولا يتراكم، لأن رقم Meta يُعاد حتى يستقرّ. `fx_rate` مخزَّن مع الصفّ (الإدخال يجري في اليوم التالي). التاريخ الافتراضي للصفحة **أمس** لا اليوم.
- **المحادثات تفصل فشل الإعلان عن فشل البيع:** محادثات رخيصة كثيرة بلا طلبات = خلل في الردّ أو السعر أو التوفّر لا في الإعلان.
- **المصروف التشغيلي الثابت (`operating_daily_costs`) لا يُوزَّع على الأصناف** — لا يتغيّر بإيقاف إعلان، وتوزيعُه يُوقف إعلانًا يساهم في تغطيته. مكانُه بطاقة اليوم، وبتاريخ سريان فلا يُعاد كتابة ربح الماضي.
- **الحكم على نافذة 3 أيام** بالطلبات المُدخَلة لا المسلَّمة (المرتجعات ≤ 5%)، مع استبعاد حالة «مُرتجَع» وخصم `returned_qty` بالتناسب. لا حكم دون 10 طلبات، إلّا استثناءين فوريّين: صرفٌ بلا محادثة، وصرفٌ ثقيل بلا طلب. ويُحجب الحكم عند ثغرة إدخالٍ داخل النافذة. العتبات كلّها من الإعدادات.
- **الصلاحيات:** `reports.ad_budget.{view,manage}` — لمدير النظام والمدير وحدهما (تكشف التكلفة والربح). في migration **و**بذرة معًا.
- **الاختبارات:** 23 اختبارًا (لقطة القناة 4، الصفحة 19) تغطّي الصلاحيات والإدخال والمرتجع الجزئي وفصل المصروف الثابت وكل فروع الحكم.

---

## المرحلة 7.3 — سحب الصرف الإعلاني من المنصّة (قراءة فقط) ✅

> **الطبقة التي تقف عليها الأتمتة لاحقًا.** الرقمان اللذان يُنسخان يدويًّا يأتيان من نداءٍ واحد على Marketing API. **خارج النطاق (بلا تنفيذ):** الكتابة إلى المنصّة (إيقاف/زيادة/إنشاء)، Pixel وCAPI، توليد المحتوى.

- **عقد ومحرّكات (ADR-052):** `AdPlatformProviderInterface` + `Null` (افتراضي آمن) و`Fake` (اختبارات) و`Meta`. **قراءة فقط بعقدٍ منفصل عن الكتابة** — رمز `ads_read` لا يملك صلاحية الصرف، فخطأ استيرادٍ لا يُنفق مالًا. الأسرار في `.env` وحدها.
- **الربط بالمعرّف (`ad_external_maps`):** الحملة ⇄ قناة، والمجموعة الإعلانية ⇄ صنف. المطابقة النصّية تُقترح (بتطبيع عربي) ولا تَربط؛ وما لا يُربَط لا يُكتَب صرفُه.
- **شاشة «الإعدادات ← ربط الحملات»:** قائمة انتظار بالمقترحات، «اربط كل المقترحات» بنقرة، تجاهُل ما لا يخصّ صنفًا، وسحبٌ فوري للتجربة.
- **`ads:sync-spend` مجدول ليليًّا** (مفعّل بالإعداد)، يُعيد سحب آخر 3 أيام لأن أرقام Meta تُراجَع بعد نشرها، ويجمع مجموعتين للصنف نفسه في صفٍّ واحد.
- **تعايش مع الإدخال اليدوي:** `source` و`synced_*` — ما أُدخل باليد يبقى، وتُعرَض قيمة المنصّة بجانبه عند الاختلاف في «الميزانية اليومية».
- **الاختبارات:** 15 اختبارًا على محرّك وهمي — لا اتصال شبكي في أي اختبار.

---

## المرحلة 7.4 — الطيّار الآلي للإعلانات (الكتابة إلى المنصّة) ✅

> **لا عقل جديد — يدٌ وقفصٌ وسجلّ.** الحكم يخرج كما هو من `AdBudgetService` المبنيّ على الربح الحقيقي بعد تكلفة الجملة والمرتجع، وهذا ما لا تعرفه القواعد الآلية في لوحة المنصّة: تلك تعرف تكلفة «التحويل» ولا تعرف ربح الصنف. **خارج النطاق (بلا تنفيذ):** إنشاء الحملات والمواد الإبداعية، رفع الميزانيات آليًّا، اختيار الأصناف وإطلاق حملاتٍ لها، Pixel وCAPI.

- **عقد كتابةٍ ثانٍ (ADR-053):** `AdPlatformWriterInterface` + `Null` (افتراضي) و`Fake` و`Meta`. رمز الكتابة (`ads_management`) منفصل عن رمز القراءة في `.env`، و**غيابه يُبقي المحرّك غير مضبوط ولو كان رمز القراءة حاضرًا**. يوقف ويشغّل ويعدّل ميزانية مجموعةٍ قائمة، ولا يُنشئ شيئًا.
- **الفرملة تُؤتمت كاملة والدوّاسة لا:** `pause` و`decrease` وحدهما، و**رفع الميزانية غير مطبَّق في أي وضع** رغم أن الحكم يقولها. إيقافٌ خاطئ يكلّف ربح يوم ويُتراجَع عنه بنقرة؛ وزيادةٌ خاطئة تصرف طوال الليل ولا تُستردّ.
- **مستويان:** `suggest` يكتب القرارات ولا يلمس المنصّة، ثم `brake` ينفّذ — فيُقرأ سجلُّ الطيّار أسبوعين قبل منحه المال.
- **القفص من اللوحة لا من الخادم** (المبدأ 8): مفتاح إطفاء، وضع التشغيل، **السقف اليومي**، أقصى نسبة تخفيض، أيام التهدئة، أدنى ميزانية، و**اختيار الصفحات** التي يديرها (عمود `ad_channels.autopilot_enabled`). والسقف يُطبَّق على المجموع بعد الخطة: يوقف الأقلّ ربحًا حتى ينزل تحته.
- **حدود تقنية لا تحفّظ:** تعديل الميزانية بأكثر من ~20% يُعيد المجموعة إلى مرحلة التعلّم لدى المنصّة — فتعديلٌ يوميّ عنيف أسوأ من عدم التدخّل. والتهدئة لتعديل الميزانية وحده؛ الإيقاف خارجها.
- **ميزانية على مستوى الحملة (CBO) لا تُمَسّ** — تعديلها يطال مجموعاتٍ رابحة؛ يُسجَّل الامتناع ويبقى الإيقاف متاحًا.
- **`ad_autopilot_decisions`:** كل قرار بأرقامه التي بُني عليها، و**يُسجَّل الامتناع كما يُسجَّل الفعل**. تراجعٌ بنقرة يعيد ما كان بالضبط، وزرٌّ أحمر يوقف كل مجموعة مربوطة فورًا.
- **`ads:autopilot` مجدول صباحًا** بعد المزامنة، وآمنٌ بلا إعداد (مطفأ افتراضيًّا ومحرّك الكتابة `null`). ومعه ملخّص يومي في الشاشة: ما أُوقف ولماذا، وما وفّره، مع أرقام اليوم المالية.
- **الصلاحيات:** `marketing.autopilot.{view,manage}` — و«الإدارة» لمدير النظام وحده، فهي الصلاحية الوحيدة التي تُنفق مالًا خارج النظام.
- **الاختبارات:** 19 اختبارًا على محرّك كتابةٍ وهمي، تحرس **ما لا يفعله** الطيّار قبل ما يفعله.

---

## المرحلة 7.5 — قياس الشراء ونسبة الطلب الإلكتروني ✅

> **شرط هدف «الشراء عبر الموقع»، لا تحسينٌ فوقه.** المنصّة لا تُحسِّن على ما لا تراه: بلا حدث شراءٍ يصلها تختار جمهورًا **ينقر** لا جمهورًا **يشتري**. وبلا نسبةٍ للطلب الإلكتروني لا يعرف أحد — طيّارًا كان أو إنسانًا — أيّ إعلانٍ أنتج أيّ طلب.

- **عقد قياس ومحرّكاته (ADR-054):** `ConversionTrackerInterface` + `Null` (افتراضي) و`Fake` و`Meta`. المتجر يعمل كاملًا بلا قياس، والأسرار في `.env`.
- **حدث الشراء من الخادم** عبر Conversions API لا من المتصفّح: مانعات الإعلانات تُسقط جزءًا كبيرًا من أحداث المتصفّح، و**مسار الإتمام محميّ لا يُمسّ** — فيُرسَل من `Order::created` في مهمّة طابور، فلا يعترض القياسُ البيعَ.
- **`event_id` من الطلب** (`purchase.{uuid}`): إعادة المحاولة لا تُحتسب شراءً ثانيًا. ويُرسَل عند الإنشاء لا عند التسليم، اتّساقًا مع أساس «الميزانية اليومية».
- **النسبة عبر `utm_content`** الذي يحمله رابط الإعلان (`{{adset.id}}`) — المنصّة لا تُخبرك بالحملة من `fbclid`. ومنه الصنف والصفحة عبر `ad_external_maps` نفسه، فلا مصدر ثانٍ للحقيقة. و`fbclid` يُحفَظ بصيغة `fb.1.{ms}.{id}` للمطابقة.
- **مساران للنسبة:** طلب الموظفة عبر حساب البزنس (كما كان)، وطلب الويب عبر المجموعة الإعلانية. وكعكة الويب لا تُطبَّق على طلب موظفة، وما لا يُربَط لا يُخمَّن.
- **بكسل المتصفّح** للزيارة و«مشاهدة الصنف» — لا يُطبَع شيء بلا معرّف مضبوط.
- **التجزئة قبل الإرسال:** بريد الزبون ورقمه يُجزّآن (SHA-256) بعد تطبيعٍ إلى الصيغة الدولية، ولا تُرسَل حقول فارغة.
- **الاختبارات:** 12 اختبارًا على محرّك قياسٍ وهمي — لا نداء شبكي.
- **باقٍ:** كتالوج المنتجات (feed)، أحداث `AddToCart`/`InitiateCheckout`، وفصل الحساب الإعلاني الثاني كيانًا (`ad_accounts`) ليعمل الطيّار عليه.

---

## المرحلة 6 (سابقًا 5) — المحاسبة ⬜

**النطاق:** دليل الحسابات، القيود اليومية (مزدوجة القيد)، فواتير المبيعات/المشتريات، المدفوعات والمقبوضات، الخزينة والبنوك، التقارير المالية.

---

## المرحلة 6 — CRM ⬜

**النطاق:** العملاء المحتملون (Leads)، جهات الاتصال، الفرص (Opportunities)، الأنشطة والمتابعات، خط أنابيب المبيعات (Pipeline).

---

## المرحلة 7 — المسوّقون والعمولات ⬜

**النطاق:** تسجيل المسوّقين، روابط/أكواد الإحالة، تتبّع الإحالات، قواعد العمولة، احتساب العمولات، طلبات السحب والمدفوعات.

---

## المرحلة 8 — الصندوق الموحّد ⬜

**النطاق:** تكامل WhatsApp Cloud API، Messenger، Instagram DM في صندوق وارد واحد، ربط المحادثات بالعملاء والطلبات، الردود والقوالب.

---

## المرحلة 9 — العروض والولاء ⬜

**النطاق:** الكوبونات، قواعد الخصم، الحملات الترويجية، نقاط الولاء واستبدالها.

---

## المرحلة 10 — التقارير والتحليلات ⬜

**النطاق:** لوحات مؤشرات الأداء (KPIs)، تقارير المبيعات/المخزون/المالية/التسويق، التصدير (PDF/Excel).

---

## المرحلة 11 — التحسين والنشر ⬜

**النطاق:** تحسين الأداء والفهرسة، مراجعة أمنية، الاختبارات، دعم تعدد الفروع، خط CI/CD، النشر للإنتاج.

---

## مبادئ عامة عبر كل المراحل

- **جودة قبل السرعة:** كود نظيف، مُختبَر، موثّق.
- **أمان أولًا:** تحقّق من المدخلات، صلاحيات صارمة، تشفير الأسرار.
- **RTL أولًا:** كل واجهة تُبنى عربية RTL ثم تُدعم بالإنجليزية.
- **قابلية التوسّع:** بنية وحدات (Modules) تسمح بإضافة ميزات دون كسر الموجود.
- **مراجعة بعد كل مرحلة:** لا ننتقل قبل اعتماد المخرجات.

</div>
