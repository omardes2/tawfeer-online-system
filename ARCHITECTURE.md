<div dir="rtl">

# المبادئ المعمارية — Tawfeer Online

هذا المستند يوثّق **القرارات المعمارية المُلزِمة** التي يجب أن يلتزم بها كل الكود منذ اليوم الأول، حتى لا نحتاج إلى إعادة تصميم النظام لاحقًا. أي انحراف عن هذه المبادئ يجب أن يُناقَش ويُوثَّق هنا أولًا.

> هذه المبادئ **ملزِمة عبر كل المراحل** في `PROJECT_PLAN.md`.

---

## 1. جاهزية تعدد الفروع (Multi-Branch Ready)

نبدأ بفرع واحد، لكن البنية تدعم عدة فروع منذ البداية.

- جدول `branches` موجود منذ المرحلة 1، ويُنشأ فرع افتراضي (Default Branch).
- كل كيان تشغيلي مرتبط بفرع (طلبات، مستودعات، خزائن، مستخدمون...) يحمل `branch_id` (nullable الآن، يُملأ بالفرع الافتراضي).
- الاستعلامات تُكتب بحيث يمكن لاحقًا تطبيق **Branch Scope** (Global Scope) دون تعديل منطق الأعمال.
- **لا نفترض أبدًا** وجود فرع واحد في الكود (لا قيم ثابتة `branch_id = 1`).

## 2. جاهزية تعدد المستودعات (Multi-Warehouse Ready)

- جدول `warehouses` يدعم عدة مستودعات، وكل مستودع مرتبط بفرع.
- المخزون يُخزَّن **لكل مستودع** (`inventory_stocks` بمفتاح مركّب variant + warehouse)، لا رصيد عام واحد.
- كل حركة مخزون (`inventory_movements`) تحدّد المستودع المصدر/الوجهة.
- التحويل بين المستودعات مدعوم كنوع حركة (`transfer`).

## 3. جاهزية تعدد المستأجرين (Multi-Tenant Ready — غير مُفعّل الآن)

نصمّم لدعم Multi-Tenant مستقبلًا **دون إعادة كتابة قاعدة البيانات**، لكن **لا نُفعّل** الميزة الآن.

- استراتيجية مُختارة: **عمود `tenant_id`** (Single-Database, shared schema) — الأرخص للترقية لاحقًا.
- الجداول القابلة للعزل تُصمَّم بحيث يمكن إضافة `tenant_id` عبر migration واحدة لاحقًا دون كسر العلاقات.
- منطق الأعمال يمرّ عبر طبقات (Repositories/Services) تسمح بحقن **Tenant Scope** لاحقًا في مكان واحد.
- **الآن:** لا يوجد `tenant_id` فعلي في الجداول؛ فقط نتجنّب أي تصميم يمنع إضافته (مثل مفاتيح فريدة عامة يصعب جعلها فريدة لكل مستأجر — نستخدم فرادة مركّبة عند الحاجة).

## 4. المعرّفات الخارجية (UUID للعناصر الحساسة)

- **مفاتيح داخلية:** `BIGINT auto-increment` (`id`) للأداء والعلاقات الداخلية.
- **معرّف خارجي:** عمود `uuid` (فريد، مفهرس) للكيانات التي تُكشف خارجيًا أو تُشارَك عبر روابط: `orders`, `invoices`, `shipments`, روابط المشاركة، والموارد المكشوفة عبر الـ API العامة.
- الروابط العامة والـ API تستخدم `uuid` لا `id` (تفادي التعداد/التخمين).
- تُطبَّق عبر تريتة `HasUuid` مشتركة تولّد الـ UUID عند الإنشاء.

## 5. الحذف الناعم (Soft Deletes)

- كل الكيانات المهمة تستخدم `SoftDeletes` (`deleted_at`): المنتجات، الطلبات، العملاء، الموردون، الفواتير، المستخدمون، الفروع، المستودعات...
- الجداول الرابطة البحتة (pivot) والسجلّات غير القابلة للاستعادة (مثل `audit_logs`, `inventory_movements`) **لا** تستخدم Soft Delete (سجلّ ثابت غير قابل للتعديل).

## 6. المبالغ المالية بنوع Decimal

- **يُمنع `float`/`double` لأي مبلغ مالي** إطلاقًا.
- كل المبالغ: `decimal(15,2)` (قابل للتوسيع إلى `decimal(19,4)` للعملات/الأسعار الدقيقة عند الحاجة).
- الكميات: `decimal(15,3)` لدعم الوحدات الكسرية (كجم، لتر).
- الحسابات المالية في PHP تستخدم `bcmath` أو Casting دقيق، لا حساب عائم.

## 7. المعاملات الذرّية (Database Transactions)

- **كل** عملية مالية أو مخزونية تتم داخل `DB::transaction()` (ذرّية: الكل أو لا شيء).
- أمثلة إلزامية: إنشاء طلب + خصم مخزون + قيد محاسبي + احتساب عمولة → معاملة واحدة.
- تُستخدم أقفال (`lockForUpdate`) عند تعديل أرصدة المخزون أو الخزائن لتفادي حالات السباق (Race Conditions).

## 8. سجلّ التدقيق المركزي (Central Audit Log)

- جدول `audit_logs` مركزي يسجّل كل عملية حسّاسة: من؟ ماذا؟ متى؟ من أين؟ القيمة قبل/بعد.
- يُطبَّق عبر تريتة `Auditable` + مراقبي النماذج (Model Observers) بحيث لا يعتمد على تذكّر المطوّر يدويًا.
- يسجّل: `user_id`, `action`, `auditable_type`, `auditable_id`, `old_values (json)`, `new_values (json)`, `ip`, `user_agent`.
- **غير قابل للتعديل/الحذف** (append-only).

## 9. نظام الإعدادات الديناميكي (Dynamic Settings)

- جدول `settings` (key/value/group) يخزّن الإعدادات في قاعدة البيانات، لا قيم ثابتة في الكود.
- طبقة `Settings` (facade/service) مع **تخزين مؤقت (cache)** للقراءة السريعة وإبطال عند التحديث.
- كل ما قد يتغيّر (العملة، الضريبة، الشحن، اللغة الافتراضية، بيانات المتجر، حدود...) يُقرأ من الإعدادات.

## 10. حالات قابلة للإدارة (Manageable Statuses)

- حالات الطلب/الدفع/الشحن **ليست ثوابت مضمّنة (enum مغلق)**، بل تُدار من لوحة التحكم.
- جداول: `order_statuses`, `payment_statuses`, `shipment_statuses` (اسم، مفتاح، لون، ترتيب، قابل للتفعيل، هل نهائي؟).
- منطق الانتقالات (State Machine) يقرأ الحالات والقواعد من قاعدة البيانات حيثما أمكن.
- تُزرع (seed) حالات افتراضية معقولة في المرحلة 1، مع بقاء الإدارة ممكنة من اللوحة.

## 11. API-First

- المنطق الأساسي (Domain/Services) منفصل عن طبقة العرض، ويُستهلك عبر **REST API** موثّق.
- مصادقة الـ API عبر **Laravel Sanctum** (توكنات) — جاهزة لتطبيق موبايل لاحقًا.
- استجابات موحّدة عبر **API Resources** (تنسيق ثابت، ترقيم، أخطاء موحّدة).
- الواجهة الويب تستهلك نفس طبقة الخدمات؛ لا منطق أعمال مكرّر في الواجهة.
- توثيق الـ API يُحدَّث مع كل وحدة.

## 12. الصلاحيات بالأدوار (Role & Permission — لا أسماء مستخدمين)

- كل تحقّق صلاحية يعتمد على **دور/صلاحية**، لا على اسم مستخدم أو `id` مضمّن.
- تُدار عبر `spatie/laravel-permission` + **Policies** لكل نموذج.
- **يُمنع** أي شرط مثل `if ($user->email === 'admin@...')`.
- الأدوار والصلاحيات قابلة للإدارة من لوحة التحكم.

## 13. طبقة التكامل (Integration/Service Layer)

- **يُمنع** ربط أي جزء بخدمة خارجية مباشرة (دفع، WhatsApp، شحن، SMS...).
- كل مزوّد خارجي خلف **واجهة (Interface/Contract)** + تنفيذ (Driver)، ويُحقَن عبر Container.
- تبديل المزوّد = إضافة Driver جديد وتغيير الإعداد، دون لمس منطق الأعمال.
- مثال: `PaymentGatewayContract` ← `TapDriver` / `HyperPayDriver`؛ `MessagingContract` ← `WhatsAppCloudDriver`.

## 14. وحدات مستقلة (Independent Modules)

- النظام مقسّم إلى **وحدات (Modules)** عالية الاستقلالية: Catalog, Inventory, Orders, Purchasing, Accounting, CRM, Affiliate, Messaging, Promotions, Reports, Admin.
- كل وحدة تحوي (قدر الإمكان): نماذجها، خدماتها، متحكماتها، مساراتها، اختباراتها.
- التواصل بين الوحدات عبر **خدمات/عقود وأحداث (Events)**، لا استدعاءات داخلية متشابكة.
- الهدف: تطوير واختبار كل وحدة بمعزل قدر الإمكان.

---

## جدول التطبيق عبر المراحل

| المبدأ | يُؤسَّس في | ملاحظة |
|--------|:---------:|--------|
| 1. تعدد الفروع | المرحلة 1 | `branches` + فرع افتراضي |
| 2. تعدد المستودعات | المرحلة 1–2 | بنية المخزون لكل مستودع |
| 3. تعدد المستأجرين | تصميم فقط | لا تفعيل الآن |
| 4. UUID | المرحلة 1 | تريتة `HasUuid` |
| 5. Soft Deletes | المرحلة 1 | على الكيانات المهمة |
| 6. Decimal | المرحلة 1 | معيار إلزامي |
| 7. Transactions | المرحلة 1 | معيار إلزامي |
| 8. Audit Log | المرحلة 1 | تريتة `Auditable` |
| 9. Settings | المرحلة 1 | جدول + طبقة + cache |
| 10. حالات ديناميكية | المرحلة 1 (بذور) | إدارة كاملة لاحقًا |
| 11. API-First | المرحلة 1 | Sanctum + Resources |
| 12. RBAC | المرحلة 1 | spatie + Policies |
| 13. طبقة التكامل | عند أول تكامل | عقود منذ البداية |
| 14. وحدات مستقلة | المرحلة 1 | بنية المجلدات |

</div>
