<?php

return [
    /*
    | المفتاح العام للوكيل. مُطفأ افتراضيًّا: تشغيل ذكاءٍ اصطناعيّ يحادث الزبائن
    | باسم الشركة قرارٌ إداريّ صريح، لا إعدادٌ يُفعَّل بالنسيان عند أول نشر.
    |
    | والإطفاء هنا يوقف **الردّ وحده**: الاستقبال والتخزين والعرض في الصندوق
    | تبقى عاملة، فلا تضيع رسالة زبونٍ لأن الوكيل مُطفأ.
    */
    'enabled' => env('AI_AGENT_ENABLED', false),

    /*
    | النموذج. `claude-sonnet-5` توازنٌ بين الجودة والتكلفة لمحادثة بيعٍ قصيرة؛
    | و`claude-haiku-4-5-20251001` أرخص للمحادثات البسيطة.
    */
    'model' => env('AI_AGENT_MODEL', 'claude-sonnet-5'),

    'max_tokens' => (int) env('AI_AGENT_MAX_TOKENS', 1024),

    /*
    | تجميع الرسائل: الزبون يرسل ثلاث رسائل متتالية، والردّ على كل واحدةٍ منفردة
    | يبدو آليًّا. تُجمَّع بتأخيرٍ يُلغى ويُعاد مع كل رسالةٍ جديدة، بسقف انتظارٍ
    | كلّي كي لا يُؤجَّل الردّ إلى ما لا نهاية على من يكتب بلا توقّف.
    */
    'debounce_seconds' => (int) env('AI_AGENT_DEBOUNCE_SECONDS', 5),
    'max_wait_seconds' => (int) env('AI_AGENT_MAX_WAIT_SECONDS', 20),

    /*
    | سقف استدعاء الأدوات في الدورة الواحدة. تجاوزه يعني أن الوكيل يدور بلا
    | وصول — فالتحويل إلى إنسانٍ أنفع من محاولةٍ ثالثة عشرة.
    */
    'max_tool_calls' => (int) env('AI_AGENT_MAX_TOOL_CALLS', 6),

    'history_messages' => (int) env('AI_AGENT_HISTORY_MESSAGES', 20),

    /*
    | مهلة نداء النموذج. الزبون ينتظر على واتساب، ونداءٌ يتجاوزها فشلٌ عمليًّا
    | ولو نجح — فالردّ بعد دقيقةٍ يصل إلى محادثةٍ انتهت.
    */
    'timeout_seconds' => (int) env('AI_AGENT_TIMEOUT_SECONDS', 30),

    /*
    | ما يُقال للزبون عند التحويل. اعتذارٌ قصير بلا سبب: أسبابُ التحويل تقنيّة
    | («تجاوز سقف الأدوات») ولا تعني الزبون، وذكرُها يقلقه بلا فائدة.
    */
    'handoff_message' => env(
        'AI_AGENT_HANDOFF_MESSAGE',
        'عذرًا، بحوّلك لموظفة تتابع معك وتردّ عليك بأقرب وقت.',
    ),

    /*
    | أسعار النموذج بالدولار لكل مليون توكن — للسجلّ لا للفوترة.
    |
    | هنا لا في الكود: تتغيّر بقرار المزوّد لا بقرارنا، وتغييرها يجب ألّا يحتاج
    | نشرًا. و`default` شبكة أمان: نموذجٌ لا سعر له يُحسب عليها، فتقديرٌ خاطئ
    | أنفع من صفرٍ يخفي الإنفاق كلّه عن شاشة التكاليف.
    */
    'pricing' => [
        'claude-sonnet-5' => ['input' => 3.00, 'output' => 15.00],
        'claude-haiku-4-5-20251001' => ['input' => 1.00, 'output' => 5.00],
        'claude-opus-5' => ['input' => 15.00, 'output' => 75.00],
        'default' => ['input' => 3.00, 'output' => 15.00],
    ],

    'api_key' => env('ANTHROPIC_API_KEY'),
];
