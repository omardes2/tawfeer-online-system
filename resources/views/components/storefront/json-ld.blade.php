@props(['data'])

{{--
    بيانات منظّمة (JSON-LD).

    ⚠️ سبب وجود هذا المكوّن: مفتاح السياق في JSON-LD يبدأ بعلامة «at»، وBlade
    يملك موجّهًا بالاسم نفسه، فكتابة المفتاح حرفيًا داخل قالب تُحقن كود PHP
    مكانه ويخرج JSON-LD تالفًا لا تقرأه محرّكات البحث. القاعدة إذًا: تُبنى
    المصفوفة داخل كتلة php (لا يمسح مُصرّف الموجّهات محتواها) ثمّ تُمرَّر إلى
    هنا. وللسبب نفسه لا يُكتب اسم أي موجّه داخل تعليقات Blade — التعليق لا
    يحمي منه، فالموجّهات تُصرَّف قبل حذف التعليقات.
--}}
<script type="application/ld+json">
{!! json_encode($data, JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES) !!}
</script>
